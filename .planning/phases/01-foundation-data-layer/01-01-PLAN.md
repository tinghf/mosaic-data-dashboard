---
phase: 01-foundation-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - README.md
  - CLAUDE.md
  - config.toml
  - .gitignore
  - .python-version
  - src/mosaic_dashboard/__init__.py
  - .planning/phases/01-foundation-data-layer/COLUMN_DISCOVERY.md
autonomous: true
requirements: [ENV-01, ENV-02]
tags: [scaffolding, uv, python, streamlit]

must_haves:
  truths:
    - "D-14: src/ layout with importable mosaic_dashboard package — a teammate can clone the repo and `uv sync` succeeds with exit code 0"
    - "D-16: pyproject.toml uses PEP 621 metadata and uv tooling; `uv.lock` is committed to git"
    - "D-17: README documents the one-liner `uv sync && uv run streamlit run src/mosaic_dashboard/app.py`"
    - "D-01 + D-02: `config.toml` exists at repo root with a `[data] root` key defaulting to `~/MOSAIC/MOSAIC-data/processed/`"
    - "Actual upstream column names per processed/ subdir are captured in a discovery note for use by Plan 03/04"
  artifacts:
    - path: "pyproject.toml"
      provides: "PEP 621 metadata + dependencies (streamlit, pandas) + uv_build backend"
      contains: "streamlit"
    - path: "uv.lock"
      provides: "Pinned dependency graph for reproducible installs"
    - path: "README.md"
      provides: "Launch one-liner and data-path setup instructions"
      contains: "uv sync && uv run streamlit run src/mosaic_dashboard/app.py"
    - path: "CLAUDE.md"
      provides: "Project conventions for downstream agents (uv-only, ISO3, read-only data, pages/ pattern)"
    - path: "config.toml"
      provides: "Repo-root project config with [data] root + [logging] level"
      contains: "[data]"
    - path: ".gitignore"
      provides: "Excludes .venv/, __pycache__, .pytest_cache, etc."
    - path: "src/mosaic_dashboard/__init__.py"
      provides: "Package marker for the installable mosaic_dashboard package"
    - path: ".planning/phases/01-foundation-data-layer/COLUMN_DISCOVERY.md"
      provides: "Recorded actual upstream column names per dataset (input for loader required-column sets)"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv sync regenerates lock from pyproject dependencies"
      pattern: "uv\\.lock"
    - from: "README.md"
      to: "src/mosaic_dashboard/app.py (created in Plan 05)"
      via: "Launch one-liner references this path"
      pattern: "src/mosaic_dashboard/app\\.py"
---

<objective>
Scaffold the `mosaic-dashboard` Python package with uv: produce an installable src/-layout project, committed lockfile, repo-root config.toml, README with launch one-liner, and a minimal CLAUDE.md locking conventions for downstream agents. Also discover and record the actual upstream CSV column names per processed/ subdir so Plans 03 and 04 can populate accurate `required_columns` sets without re-reading source files.

Purpose: Satisfy ENV-01 (uv + pinned deps + committed uv.lock) and ENV-02 (documented one-liner launches dashboard). Without this scaffold, no other plan can land code that runs.

Output: A repository where `uv sync` succeeds, the package `mosaic_dashboard` is importable, and downstream plans (02-05) have a verified pyproject + lockfile + project conventions doc + column-name reference to build against.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize uv package and install runtime dependencies</name>
  <files>pyproject.toml, uv.lock, .python-version, src/mosaic_dashboard/__init__.py, .gitignore, README.md</files>
  <read_first>
    - `.planning/phases/01-foundation-data-layer/01-CONTEXT.md` (D-14, D-16, D-17 lock src/ layout, installable package, launch one-liner)
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Project Scaffolding" (exact scaffolding command sequence, resulting pyproject.toml shape) and §"Standard Stack" (streamlit 1.57, pandas 3.0, uv_build backend)
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Pitfalls" P10 (uv init --package overwriting README — run exactly once)
  </read_first>
  <action>
    Run `uv init --package --name mosaic-dashboard --python 3.13` from the repo root (verified `command -v uv` per RESEARCH.md §"Environment Availability"). This creates `pyproject.toml`, `.python-version`, `src/mosaic_dashboard/__init__.py`, and may create a `README.md` stub. Implements D-14 (src/ layout) + D-16 (installable package + uv_build backend) per RESEARCH.md §"Project Scaffolding".

    Then run `uv add streamlit pandas` to declare runtime deps under `[project] dependencies`. Per RESEARCH.md §"Standard Stack", target version ranges `streamlit>=1.57,<2.0` and `pandas>=3.0,<4.0`; if `uv add` writes broader/narrower bounds, hand-edit pyproject.toml to match. Do NOT add `tomli` or `pandera` or `structlog` — RESEARCH.md alternatives table explicitly rejects them for Phase 1 (stdlib `tomllib`/`logging` suffice; pandera/structlog are deferred).

    Run `uv sync` to generate `uv.lock` and install the project editably into `.venv/`. Verify exit code 0.

    Write `.gitignore` covering `.venv/`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `.streamlit/secrets.toml`, and `dist/build/*.egg-info/`. Verify `.venv/` is in `.gitignore` per RESEARCH.md §"Runtime State Inventory".

    If `uv init --package` did not produce a README.md, create a minimal `README.md` placeholder (Task 2 will fill it out). If a README was created, leave it for Task 2 to overwrite.

    Final pyproject.toml [project] section must contain `name = "mosaic-dashboard"`, `requires-python = ">=3.11"`, and dependencies for streamlit + pandas. The [build-system] section must use `uv_build` per RESEARCH.md §"Project Scaffolding" "Resulting pyproject.toml shape".
  </action>
  **Decisions addressed:** D-14 (src/ layout with importable `mosaic_dashboard` package), D-16 (`pyproject.toml` PEP 621 metadata managed by uv with committed `uv.lock`) — see CONTEXT.md.
  <verify>
    <automated>uv sync 2>&amp;1 | tee /tmp/uv-sync.log; test -f uv.lock &amp;&amp; test -f pyproject.toml &amp;&amp; test -f src/mosaic_dashboard/__init__.py &amp;&amp; grep -q "streamlit" pyproject.toml &amp;&amp; grep -q "pandas" pyproject.toml &amp;&amp; grep -q "uv_build" pyproject.toml &amp;&amp; grep -q "^\.venv/$" .gitignore &amp;&amp; echo "TASK1 OK" || echo "TASK1 FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` exists with `[project] name = "mosaic-dashboard"` and `requires-python = ">=3.11"`
    - `pyproject.toml` lists `streamlit` and `pandas` in `[project] dependencies`
    - `pyproject.toml` `[build-system]` uses `uv_build` as the build backend
    - `uv.lock` exists and is non-empty
    - `uv sync` exits with status 0
    - `src/mosaic_dashboard/__init__.py` exists
    - `.python-version` exists and contains `3.13`
    - `.gitignore` contains a line for `.venv/`
    - `uv run python -c "import mosaic_dashboard"` exits 0 (package is importable)
  </acceptance_criteria>
  <done>uv-managed installable package exists, runtime deps installed, uv.lock committed-ready, package importable from the venv.</done>
</task>

<task type="auto">
  <name>Task 2: Author config.toml, README, and CLAUDE.md</name>
  <files>config.toml, README.md, CLAUDE.md</files>
  <read_first>
    - `.planning/phases/01-foundation-data-layer/01-CONTEXT.md` (D-01: repo-root config.toml; D-02: default ~/MOSAIC/MOSAIC-data/processed/; D-17: launch one-liner; "Claude's Discretion" allows minimal CLAUDE.md)
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Project Scaffolding" subsection "config.toml (repo root)" for the exact TOML shape
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Pitfalls" P4 (config.toml vs .streamlit/config.toml distinction) and P5 (WSL2 path variation)
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Open Questions" item 5 (CLAUDE.md recommendation: launch one-liner, read-only data, ISO3 canonical, uv-only)
  </read_first>
  <action>
    Create `config.toml` at the repo root (NOT under `.streamlit/`) with two sections per RESEARCH.md §"Project Scaffolding":
    - `[data]` section with `root = "~/MOSAIC/MOSAIC-data/processed/"` (the D-02 default).
    - `[logging]` section with `level = "INFO"` (consumed by `logging_config.configure()` in Plan 02).
    Add a header comment loud about P4: "This file configures the Mosaic Dashboard application, NOT Streamlit itself. Streamlit's own runtime settings live under `.streamlit/config.toml`."

    Overwrite `README.md` with a focused project README covering:
    - One-paragraph "what this is" (pull a short version from PROJECT.md).
    - Quickstart section with the exact one-liner from D-17: `uv sync && uv run streamlit run src/mosaic_dashboard/app.py` (per ENV-02).
    - "Configuring the data path" section explaining the three-level resolution per D-04 (sidebar override → `config.toml` → `~/MOSAIC/MOSAIC-data/processed/` default) and noting that WSL2 / different layouts edit `config.toml` (P5 mitigation).
    - "Project layout" section listing `src/mosaic_dashboard/{app.py,config.py,data/,ui/,pages/}` and `config.toml` at repo root.

    Create `CLAUDE.md` at the repo root with the four conventions from RESEARCH.md §"Open Questions" item 5:
    1. Launch via `uv run streamlit run src/mosaic_dashboard/app.py` (never bare `streamlit run`; per P2).
    2. `~/MOSAIC/MOSAIC-data/` is READ-ONLY — never write under that path (per PROJECT.md constraint and D-19).
    3. ISO3 is the canonical country identifier across the whole project (per D-07); upstream column names get renamed inside loaders.
    4. `uv` is the only package manager. No `pip install`, no `conda`. Pages live under `src/mosaic_dashboard/pages/` (per D-15); no third-party page-routing libs.
    Keep CLAUDE.md ~30-40 lines — conventions only, not documentation.
  </action>
  **Decisions addressed:** D-01 (committed `config.toml` at repo root), D-02 (default `~/MOSAIC/MOSAIC-data/processed/`), D-17 (README documents the `uv sync && uv run streamlit run ...` launch one-liner) — see CONTEXT.md. CLAUDE.md additionally surfaces D-07 (ISO3 canonical) and D-15 (Streamlit native `pages/`) as project conventions for downstream agents.
  <verify>
    <automated>test -f config.toml &amp;&amp; test -f README.md &amp;&amp; test -f CLAUDE.md &amp;&amp; grep -q '^\[data\]' config.toml &amp;&amp; grep -q '^root = "~/MOSAIC/MOSAIC-data/processed/"' config.toml &amp;&amp; grep -q '^\[logging\]' config.toml &amp;&amp; grep -q 'uv sync &amp;&amp; uv run streamlit run src/mosaic_dashboard/app.py' README.md &amp;&amp; grep -q 'ISO3' CLAUDE.md &amp;&amp; grep -q 'READ-ONLY\|read-only' CLAUDE.md &amp;&amp; echo "TASK2 OK" || echo "TASK2 FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `config.toml` exists at repo root with `[data]` section containing `root = "~/MOSAIC/MOSAIC-data/processed/"`
    - `config.toml` contains `[logging]` section with `level = "INFO"`
    - `config.toml` header comment explicitly distinguishes it from `.streamlit/config.toml` (P4 mitigation language present)
    - `README.md` contains the exact string `uv sync && uv run streamlit run src/mosaic_dashboard/app.py`
    - `README.md` documents the three-level data-path resolution order
    - `CLAUDE.md` mentions: `uv run` (not bare streamlit), read-only data, ISO3 canonical, and uv-only/pages-directory conventions
    - `uv run python -c "import tomllib; cfg = tomllib.load(open('config.toml','rb')); assert cfg['data']['root'] == '~/MOSAIC/MOSAIC-data/processed/'; assert cfg['logging']['level'] == 'INFO'"` exits 0
  </acceptance_criteria>
  <done>config.toml, README.md, and CLAUDE.md committed-ready; downstream agents have launch instructions and convention reference; config.toml parses cleanly with tomllib.</done>
</task>

<task type="auto">
  <name>Task 3: Discover and record upstream CSV column names</name>
  <files>.planning/phases/01-foundation-data-layer/COLUMN_DISCOVERY.md</files>
  <read_first>
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Open Questions" item 1 (recommendation to discover actual upstream column names in Wave 0; ~30 min of work; de-risks every loader)
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Assumptions Log" rows A1, A2 (loader `required_columns` sets are placeholder until real columns observed)
    - `.planning/phases/01-foundation-data-layer/01-CONTEXT.md` (D-06: loaders own rename/reshape; D-07: ISO3 canonical join column)
  </read_first>
  <action>
    For each of the 10 `~/MOSAIC/MOSAIC-data/processed/` subdirs, read the header row (and 1-2 sample data rows where useful) of every primary CSV present and record findings in `.planning/phases/01-foundation-data-layer/COLUMN_DISCOVERY.md`. Use `head -2` via Bash (read-only operation — never modify the upstream files per PROJECT.md/D-19).

    For each subdir, capture:
    - Subdir name (e.g., `WHO/annual/`)
    - File name(s) actually present (e.g., `cholera_country_year_*.csv` or whatever shipped)
    - Header row verbatim (comma-separated)
    - The country-identifier column name in the upstream CSV (if any) — this is what loaders will rename to `country_iso3` per D-07
    - Recommended `required_columns` set for the loader (post-rename, using `country_iso3` as the canonical name)
    - Whether a date/time column exists and what its native name is (for `parse_dates=` in pandas read_csv per P6)

    Cover all 10 subdirs:
    - `WHO/annual/`, `WHO/weekly/`, `WHO/daily/`
    - `WASH/` (single file `WASH_data_Sikder_2023.csv` per CONTEXT.md canonical_refs)
    - `ENSO/` (3 files: `compiled_ENSO_1970_2025_{daily,weekly,monthly}.csv` per CONTEXT.md canonical_refs)
    - `demographics/` (`UN_world_population_prospects_1967_2100.csv`, `demographics_africa_2000_2023.csv`)
    - `OAG/` (daily/weekly/monthly 2017 means)
    - `shapefiles/` (filename pattern only — no columns; record ISO3 prefix convention `XXX_ADM0.{shp,shx,dbf,prj}`)
    - `immunity/`, `vaccine_effectiveness/`, `symptomatic/`, `similarity_matrix/`

    If a subdir's expected files are missing, record "MISSING" and proceed. Plan 03/04 loaders for those subdirs will then be `_empty_*()`-only stubs with a placeholder `required_columns` set (TODO-tagged) instead of full impls.

    Use Markdown tables for clarity. Header structure:
    ```
    ## {subdir}/
    **Files present:** ...
    **Upstream columns (verbatim):** ...
    **Country column (upstream → canonical):** `iso_code` → `country_iso3` (example)
    **Date column (upstream → canonical):** `date` → `date` (parse_dates)
    **Recommended required_columns set (post-rename):** {"country_iso3", "year", "cases"}
    **Sample row:** ...
    ```
  </action>
  **Decisions addressed:** Captures the ground truth that downstream loaders rely on for D-06 (loader-owned rename/reshape) and D-07 (ISO3 canonical join key) — see CONTEXT.md.
  <verify>
    <automated>test -f .planning/phases/01-foundation-data-layer/COLUMN_DISCOVERY.md &amp;&amp; for sub in WHO/annual WHO/weekly WHO/daily WASH ENSO demographics OAG shapefiles immunity vaccine_effectiveness symptomatic similarity_matrix; do grep -q "## ${sub}" .planning/phases/01-foundation-data-layer/COLUMN_DISCOVERY.md || { echo "MISSING SECTION: $sub"; exit 1; }; done &amp;&amp; grep -qi "required_columns" .planning/phases/01-foundation-data-layer/COLUMN_DISCOVERY.md &amp;&amp; echo "TASK3 OK" || echo "TASK3 FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `COLUMN_DISCOVERY.md` exists in the phase directory
    - File contains a section for each of these 12 subdir patterns: `WHO/annual`, `WHO/weekly`, `WHO/daily`, `WASH`, `ENSO`, `demographics`, `OAG`, `shapefiles`, `immunity`, `vaccine_effectiveness`, `symptomatic`, `similarity_matrix`
    - Each section identifies (or marks "MISSING") the actual files present in that subdir
    - Each section records a recommended `required_columns` set or marks the loader for stub-only impl
    - Each section identifies the upstream country-identifier column name (where applicable) for the `country_iso3` rename
    - No upstream files under `~/MOSAIC/MOSAIC-data/processed/` are modified (verify: `git -C ~/MOSAIC/MOSAIC-data status` shows clean if MOSAIC-data is a git repo, else check mtimes are unchanged)
  </acceptance_criteria>
  <done>Downstream loader plans have ground-truth upstream column names to populate accurate required_columns sets and parse_dates lists. No upstream files mutated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → uv/PyPI | One-time `uv sync` fetches deps over network; not in hot path (DATA-03 unaffected). |
| repo → `~/MOSAIC/MOSAIC-data/` | Read-only access for column discovery; never written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Upstream `processed/` files during column discovery | mitigate | Discovery uses `head -2` reads only; verify upstream mtimes unchanged after task; CLAUDE.md codifies read-only constraint for future agents. |
| T-01-02 | Information Disclosure | `uv.lock` committed | accept | `uv.lock` records public PyPI hashes only; no secrets or PII. |
| T-01-03 | Tampering | `pyproject.toml` build-system config | mitigate | Pin `uv_build>=0.11.14,<0.12` exactly as RESEARCH.md §"Project Scaffolding" specifies; rely on uv.lock for hash-verified installs. |
</threat_model>

<verification>
After all three tasks:
1. `uv sync` exits 0 from a clean state — pyproject + lockfile are consistent.
2. `uv run python -c "import mosaic_dashboard; import streamlit; import pandas"` exits 0.
3. `cat config.toml | uv run python -c "import sys, tomllib; cfg = tomllib.loads(sys.stdin.read()); assert 'data' in cfg and 'root' in cfg['data']; print('OK')"` prints OK.
4. `grep -c '^##' .planning/phases/01-foundation-data-layer/COLUMN_DISCOVERY.md` returns at least 12 (one section per subdir pattern).
</verification>

<success_criteria>
- ENV-01 satisfied: `uv.lock` committed, pyproject.toml with `[build-system]` + pinned deps.
- ENV-02 satisfied: README contains the launch one-liner exactly as in D-17.
- Downstream agents (Plans 02-05) can rely on: `mosaic_dashboard` package importable, `config.toml` parseable, COLUMN_DISCOVERY.md as ground-truth for upstream column names.
- No upstream MOSAIC-data files modified (read-only constraint preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-layer/01-01-SUMMARY.md` documenting:
- The final pyproject.toml shape (deps, build-backend, requires-python)
- The exact upstream column names recorded per subdir (or a pointer to COLUMN_DISCOVERY.md)
- Any deviations from the RESEARCH.md scaffolding sequence and why
- Confirmation that uv.lock + the package are in place for Plan 02 to build foundation modules
</output>
</content>
</invoke>