---
phase: 01-foundation-data-layer
plan: 04
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/mosaic_dashboard/data/shapefiles.py
  - src/mosaic_dashboard/data/immunity.py
  - src/mosaic_dashboard/data/vaccine_effectiveness.py
  - src/mosaic_dashboard/data/symptomatic.py
  - src/mosaic_dashboard/data/similarity_matrix.py
autonomous: true
requirements: [DATA-02, DATA-03, DATA-04]
tags: [data-layer, loaders, stubs, pandas]

must_haves:
  truths:
    - "D-05 + D-08 + D-10: Each small-five loader (one module per subdir, named loader fns) returns a pandas DataFrame on the happy path AND in the missing-subdir/missing-country case"
    - "D-06 + D-07 + D-12 + D-18: Each loader uses the same `resolve_data_root()` + `require_columns()` + `@st.cache_data(mtime)` pattern as the big-five, normalizing to canonical `country_iso3` columns and raising on schema mismatch"
    - "D-07: `shapefiles.available_countries()` returns a list of ISO3 strings parsed from `XXX_ADM0.shp` filenames in `processed/shapefiles/`"
    - "D-20: Phase 5 can flesh out the four 'stub-with-discovery' loaders by adding the rename+schema-check inside `_read_*_cached`; the API contract does not change (reversible internal representation)"
    - "D-19: No geopandas / fiona / shapely dependency is added in Phase 1 (plain pandas + Path-only filename inspection)"
  artifacts:
    - path: "src/mosaic_dashboard/data/shapefiles.py"
      provides: "available_countries() + load_africa() + load_country(country) — Path-only ops, no geopandas"
      contains: "def available_countries"
    - path: "src/mosaic_dashboard/data/immunity.py"
      provides: "load(country) — stub-with-discovery"
      contains: "def load"
    - path: "src/mosaic_dashboard/data/vaccine_effectiveness.py"
      provides: "load(country) — stub-with-discovery"
      contains: "def load"
    - path: "src/mosaic_dashboard/data/symptomatic.py"
      provides: "load(country) — stub-with-discovery"
      contains: "def load"
    - path: "src/mosaic_dashboard/data/similarity_matrix.py"
      provides: "load() — full matrix, view slices"
      contains: "def load"
  key_links:
    - from: "data/shapefiles.py:available_countries()"
      to: "<root>/shapefiles/*_ADM0.shp"
      via: "Path.glob('*_ADM0.shp') → extract ISO3 prefix from filename"
      pattern: "_ADM0\\.shp"
    - from: "small-five loaders"
      to: "src/mosaic_dashboard/config.py:resolve_data_root()"
      via: "Each loader resolves root before checking subdir"
      pattern: "resolve_data_root"
    - from: "small-five loaders"
      to: "src/mosaic_dashboard/data/_schema.py:require_columns()"
      via: "Cached readers call require_columns() once full impl lands in Phase 5"
      pattern: "require_columns"
---

<objective>
Implement the five "small/static" loader modules under `src/mosaic_dashboard/data/`. These cover the remaining `processed/` subdirs that Phase 5 will visualize: `shapefiles/`, `immunity/`, `vaccine_effectiveness/`, `symptomatic/`, `similarity_matrix/`. In Phase 1, each loader exposes its full API contract (signature, return type) and produces a working empty-state for the Data Status page (Plan 05) to enumerate. For `shapefiles.py`, also implement `available_countries()` because it's Path-only (no geopandas needed) and gets used immediately by Plan 05's Data Status page to confirm shapefile presence. For the other four, Phase 5 fleshes out the rename/schema-check/visualization details — but the API contract is locked here so Plans 02-7 can rely on it.

Purpose: Same DATA-02 / DATA-03 / DATA-04 satisfaction as Plan 03, but for the small/static layers. Crucially, this plan runs in PARALLEL with Plan 03 (both depend only on Plan 02; their `files_modified` sets are disjoint).

Output: Five Python modules. Plan 05's Data Status page enumerates all 10 subdirs by composing loaders + Path-only metadata. Phase 5's per-layer views fill in the rendering details.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-02-SUMMARY.md
@.planning/phases/01-foundation-data-layer/COLUMN_DISCOVERY.md
@src/mosaic_dashboard/config.py
@src/mosaic_dashboard/data/errors.py
@src/mosaic_dashboard/data/_schema.py

<interfaces>
<!-- Foundation contracts from Plan 02 (same as Plan 03). -->

```python
from mosaic_dashboard.config import resolve_data_root  # () -> Path
from mosaic_dashboard.data._schema import require_columns  # (df, required, dataset) -> None | raises
```

<!-- "Stub-with-discovery" pattern explained:

For immunity/vaccine_effectiveness/symptomatic/similarity_matrix, Phase 1's loader:
  1. Resolves the data root.
  2. Checks the subdir exists (warn + return empty if not — same as big-five).
  3. Lists files in the subdir (returns empty if none).
  4. If COLUMN_DISCOVERY.md found actual upstream files, the loader CAN do a basic
     `pd.read_csv` + minimal column-existence check. But: required_columns may be
     a placeholder set (e.g., {"country_iso3"} only); Phase 5 expands it with the
     full canonical set when implementing the visualization.
  5. If COLUMN_DISCOVERY.md marked the subdir as "MISSING upstream" (no files
     found during discovery), the loader is empty-only — _read_*_cached exists but
     is never reached because step 3 returns empty first. A top-of-file
     `# TODO(Phase 5): full impl after upstream layout confirmed` comment marks
     this.
-->

Phase 1 contract for each small-five loader:
- `shapefiles.available_countries() -> list[str]` (ISO3 codes parsed from filenames)
- `shapefiles.load_africa() -> pd.DataFrame` (presence/mtime metadata of AFRICA_ADM0.* files; geopandas Phase 2)
- `shapefiles.load_country(country: str) -> pd.DataFrame` (presence/mtime metadata of <country>_ADM0.* files; geopandas Phase 2)
- `immunity.load(country: str | None = None) -> pd.DataFrame`
- `vaccine_effectiveness.load(country: str | None = None) -> pd.DataFrame`
- `symptomatic.load(country: str | None = None) -> pd.DataFrame`
- `similarity_matrix.load() -> pd.DataFrame` (no country filter — full matrix; views slice)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement shapefiles.py (Path-only, no geopandas)</name>
  <files>src/mosaic_dashboard/data/shapefiles.py</files>
  <read_first>
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Per-subdir loader module map" — `shapefiles.py` exposes `load_africa()` and `load_country(country)`; "Returns GeoDataFrame-or-DataFrame in Phase 1; geopandas not required for 'list/check existence' only."
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Open Questions" item 3 — explicit recommendation: "Use `Path.glob('*_ADM0.shp')` in Phase 1. Defer geopandas to Phase 2. The `shapefiles.py` loader exposes only `available_countries()` + presence/mtime metadata in Phase 1; full geometry reads come in Phase 2."
    - `.planning/phases/01-foundation-data-layer/01-CONTEXT.md` (D-07: ISO3 canonical; canonical_refs section: shapefile naming is `XXX_ADM0.{shp,shx,dbf,prj}` and `AFRICA_ADM0.{shp,shx,dbf,prj}`).
    - `.planning/phases/01-foundation-data-layer/COLUMN_DISCOVERY.md` — confirms shapefile filename pattern and observed ISO3 prefixes.
  </read_first>
  <action>
    Implement `src/mosaic_dashboard/data/shapefiles.py` WITHOUT any geopandas/fiona/shapely import (per RESEARCH.md §"Open Questions" item 3 — geopandas defers to Phase 2).

    Public functions:

    **`available_countries() -> list[str]`** — Returns the sorted list of ISO3 country codes present in `<root>/shapefiles/` based on filename pattern `XXX_ADM0.shp`. Implementation:
    - `root = resolve_data_root()`
    - `subdir = root / "shapefiles"`
    - If subdir doesn't exist: `logging.warning("shapefiles/ not found at %s", subdir)` and return `[]`.
    - Glob `*_ADM0.shp` filenames.
    - For each filename `XXX_ADM0.shp`, extract the prefix before `_ADM0.shp`. Filter out `AFRICA` (not a country code).
    - Return `sorted(unique_prefixes)`.
    - Validate prefix is a 3-letter uppercase alphabetic string (defensive — skip anything that isn't ISO3-shaped, e.g., a stray `.shp` from a different naming scheme).

    **`load_africa() -> pd.DataFrame`** — Phase 1 returns a metadata DataFrame describing the four AFRICA_ADM0.* sidecar files (presence + mtime). Phase 2 replaces this with a GeoDataFrame returning actual geometries. Schema for Phase 1:
    - Columns: `{"filename": str, "extension": str, "exists": bool, "mtime": float | None, "size_bytes": int | None}`.
    - Rows: one per sidecar (`AFRICA_ADM0.shp`, `AFRICA_ADM0.shx`, `AFRICA_ADM0.dbf`, `AFRICA_ADM0.prj`).
    - If `<root>/shapefiles/` missing: warn + return DF with all four rows where `exists=False`.

    **`load_country(country: str) -> pd.DataFrame`** — Like `load_africa()` but for `<country>_ADM0.*` per-country sidecars. Schema identical to `load_africa()`. If the country has no shapefile (e.g., `country='ZZZ'`), return a DF with `exists=False` for all four extensions.

    Do NOT cache these functions with `@st.cache_data` — they perform `Path.stat()` calls which are already O(1) and we want fresh mtime data on every Data Status reload. Caching would defeat that.

    Use the same `logging.getLogger(__name__)` pattern as `who.py`.

    Add a top-of-file module docstring noting:
    > "Phase 1 returns presence/mtime metadata only — no shapefile parsing. Phase 2 (SSA map) replaces these with geopandas reads of the actual `.shp` geometries. The function signatures stay stable; only the return-DataFrame schema gains geometry columns."
  </action>
  **Decisions addressed:** D-05 (one `shapefiles` module per `shapefiles/` subdir with named functions `available_countries`/`load_africa`/`load_country`), D-07 (ISO3 codes parsed from `XXX_ADM0.shp` filenames as the canonical country identifier), D-10 (missing subdir → empty list / metadata DF with `exists=False` + `logging.warning`), D-19 (Phase 1 stays plain-pandas — no geopandas/fiona/shapely; Phase 2 will introduce geopandas), D-20 (Phase 2 can swap `load_africa`/`load_country` internals to GeoDataFrame reads without changing the public signature) — see CONTEXT.md.
  <verify>
    <automated>uv run python -c "
import inspect
import pandas as pd
import streamlit as st
from mosaic_dashboard.data import shapefiles

class FakeSessionState(dict):
    def get(self, k, default=None): return super().get(k, default)
st.session_state = FakeSessionState()

# Signatures
sig = inspect.signature(shapefiles.available_countries)
assert len(sig.parameters) == 0, 'available_countries takes no args'
sig = inspect.signature(shapefiles.load_country)
assert 'country' in sig.parameters, 'load_country takes country'

# No geopandas import (verify by checking module attributes)
import sys
mod = sys.modules['mosaic_dashboard.data.shapefiles']
mod_text = open(mod.__file__).read()
assert 'geopandas' not in mod_text, 'shapefiles.py must not import geopandas in Phase 1'
assert 'import fiona' not in mod_text, 'shapefiles.py must not import fiona in Phase 1'
assert 'import shapely' not in mod_text, 'shapefiles.py must not import shapely in Phase 1'

# Real path: available_countries returns ISO3 codes
import os
real_root = os.path.expanduser('~/MOSAIC/MOSAIC-data/processed')
if os.path.exists(os.path.join(real_root, 'shapefiles')):
    countries = shapefiles.available_countries()
    assert isinstance(countries, list), f'expected list, got {type(countries)}'
    assert all(len(c) == 3 and c.isalpha() and c.isupper() for c in countries), f'non-ISO3 entries: {countries}'
    assert 'AGO' in countries or 'BEN' in countries, f'expected sample ISO3, got {countries[:10]}'
    assert 'AFRICA' not in countries, 'AFRICA must be filtered out'

# Missing-subdir: empty list, no crash
import tempfile
with tempfile.TemporaryDirectory() as tmp:
    st.session_state['data_root_override'] = tmp
    assert shapefiles.available_countries() == [], 'missing shapefiles/ should return []'
    df = shapefiles.load_africa()
    assert isinstance(df, pd.DataFrame), f'load_africa: expected DataFrame, got {type(df)}'
    assert not df.empty, 'load_africa returns metadata rows even on missing subdir'
    assert (df['exists'] == False).all(), 'all exists=False when subdir missing'

print('TASK1 OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `src/mosaic_dashboard/data/shapefiles.py` exists and does NOT contain the strings `geopandas`, `import fiona`, or `import shapely`
    - Exports `available_countries() -> list[str]`, `load_africa() -> pd.DataFrame`, `load_country(country: str) -> pd.DataFrame`
    - When pointed at the real `~/MOSAIC/MOSAIC-data/processed/`, `available_countries()` returns a non-empty list of 3-letter uppercase ISO3 codes
    - The string `'AFRICA'` is excluded from `available_countries()` output (it's the regional aggregate, not a country)
    - When pointed at an empty tempdir, `available_countries()` returns `[]` (no exception)
    - `load_africa()` and `load_country()` return DataFrames with columns `{filename, extension, exists, mtime, size_bytes}` regardless of subdir presence
    - The verify command's assertions pass
  </acceptance_criteria>
  <done>Shapefiles metadata-only loader is in place. `available_countries()` is the integration point Plan 05's Data Status uses to confirm shapefile presence; Phases 2 + 6 will extend (not replace) the loader for actual geometry reads.</done>
</task>

<task type="auto">
  <name>Task 2: Implement immunity, vaccine_effectiveness, symptomatic loaders</name>
  <files>src/mosaic_dashboard/data/immunity.py, src/mosaic_dashboard/data/vaccine_effectiveness.py, src/mosaic_dashboard/data/symptomatic.py</files>
  <read_first>
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Per-subdir loader module map" — each of these three modules exposes a single `load(country)` function. "Static layer."
    - `.planning/phases/01-foundation-data-layer/COLUMN_DISCOVERY.md` — sections for `immunity/`, `vaccine_effectiveness/`, `symptomatic/`. Use the recorded `required_columns` set (placeholder `{"country_iso3"}` if discovery marked the subdir as MISSING; full set if discovery found actual files).
    - `src/mosaic_dashboard/data/wash.py` (created in Plan 03) — single-function loader template. Mirror its shape exactly.
    - `.planning/phases/01-foundation-data-layer/01-CONTEXT.md` (D-08, D-10, D-12 — same as Plan 03).
  </read_first>
  <action>
    For each of the three modules `immunity.py`, `vaccine_effectiveness.py`, `symptomatic.py`, implement the single-function loader pattern (identical to `wash.py` from Plan 03, with a different subdir).

    Each module exports:
    - Module-level `<NAME>_REQUIRED_COLUMNS: set[str]` constant. Populate from COLUMN_DISCOVERY.md. If discovery found real files and a real upstream country column → full set including `country_iso3` and the actual value column name(s). If discovery marked the subdir as MISSING upstream → placeholder `{"country_iso3"}` plus a `# TODO(Phase 5):` comment.
    - Public `load(country: str | None = None) -> pd.DataFrame` following the standard template: resolve root → check subdir exists (warn + empty if not) → list CSVs (return empty if none) → take canonical file (sorted glob first, OR named file from COLUMN_DISCOVERY) → call cached reader → optional country filter → reset_index → return.
    - Private `_read_<name>_cached(path: str, mtime: float) -> pd.DataFrame` with `@st.cache_data(show_spinner=False)`. Performs `pd.read_csv` (with `parse_dates=` only if a date column was discovered) → rename (if discovery captured rename map) → `require_columns(df, REQUIRED_COLUMNS, dataset="<name>")` → return.
    - Private `_empty() -> pd.DataFrame` returning a DF with the canonical columns.

    The `dataset=` arg to `require_columns()` is `"immunity"`, `"vaccine_effectiveness"`, `"symptomatic"` respectively (matching the subdir name; this surfaces in the SchemaMismatchError message).

    All three use the same `logging.getLogger(__name__)` pattern.

    Phase 5 will revisit:
    - `vaccine_effectiveness/` and `symptomatic/` may not have a per-country breakdown — RESEARCH.md describes them as "best-effort visualization". If COLUMN_DISCOVERY.md captured columns suggesting non-per-country values (e.g., just `value` and `parameter`), the `country` filter step becomes a no-op (skip the filter, return the full DF). Document this in the loader's docstring.
    - `similarity_matrix/` (Task 3 below) is a special case — full matrix, no country filter.
  </action>
  **Decisions addressed:** D-05 (one module per subdir with named loader fn — `immunity.load`, `vaccine_effectiveness.load`, `symptomatic.load`), D-06 (rename map inside `_read_*_cached` per COLUMN_DISCOVERY.md), D-07 (`country_iso3` canonical join key), D-08 (country absent → empty DataFrame), D-10 (missing subdir → empty + warning), D-12 + D-13 (strict on schema mismatch, lenient on absence), D-18 (`@st.cache_data` mtime-keyed), D-19 (plain `pd.read_csv` only), D-20 (public/private split: Phase 5 can tighten `_read_*_cached` without changing `load()` signatures) — see CONTEXT.md.
  <verify>
    <automated>uv run python -c "
import inspect
import pandas as pd
import streamlit as st
from mosaic_dashboard.data import immunity, vaccine_effectiveness, symptomatic
from mosaic_dashboard.data.errors import SchemaMismatchError

class FakeSessionState(dict):
    def get(self, k, default=None): return super().get(k, default)
st.session_state = FakeSessionState()

# All three export load() taking country
for mod, name in [(immunity, 'immunity'), (vaccine_effectiveness, 've'), (symptomatic, 'symptomatic')]:
    fn = mod.load
    sig = inspect.signature(fn)
    assert 'country' in sig.parameters, f'{name}.load must accept country'

# Missing-subdir empty-state for all three
import tempfile
with tempfile.TemporaryDirectory() as tmp:
    st.session_state['data_root_override'] = tmp
    for mod, name in [(immunity, 'immunity'), (vaccine_effectiveness, 've'), (symptomatic, 'sympt')]:
        df = mod.load('AGO')
        assert isinstance(df, pd.DataFrame), f'{name}: expected DataFrame, got {type(df)}'
        assert df.empty, f'{name}: expected empty DF, got len={len(df)}'

# Cached readers exist and are @st.cache_data wrappers
for mod in (immunity, vaccine_effectiveness, symptomatic):
    cached_fns = [v for k, v in vars(mod).items() if k.startswith('_read_') and k.endswith('_cached')]
    assert len(cached_fns) == 1, f'{mod.__name__}: expected exactly 1 _read_*_cached, found {len(cached_fns)}'
    cf = cached_fns[0]
    assert hasattr(cf, 'clear'), f'{mod.__name__}: cached fn missing .clear() — not @st.cache_data?'

print('TASK2 OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `src/mosaic_dashboard/data/immunity.py`, `vaccine_effectiveness.py`, `symptomatic.py` each export `load(country: str | None = None) -> pd.DataFrame`
    - Each module defines a module-level `REQUIRED_COLUMNS` constant (the actual name may be `IMMUNITY_REQUIRED_COLUMNS` etc.) containing at minimum `{"country_iso3"}` (if discovery was a stub) or the full set from COLUMN_DISCOVERY.md
    - Each module defines exactly one `_read_*_cached` function decorated with `@st.cache_data` (verified by `.clear()` method presence)
    - With `resolve_data_root()` pointing at an empty tempdir, all three `load('AGO')` calls return an empty `pd.DataFrame` (no exception)
    - Each cached reader signature is `(path: str, mtime: float)` (no `Path` objects per P7)
    - Each loader uses `logging.getLogger(__name__)` and emits a warning when its subdir is absent
    - The verify command's assertions pass
  </acceptance_criteria>
  <done>Three single-function loaders ship with the canonical empty-state pattern. Plan 05 lists them on the Data Status page; Phase 5 fleshes them out with real renames and visualizations.</done>
</task>

<task type="auto">
  <name>Task 3: Implement similarity_matrix loader</name>
  <files>src/mosaic_dashboard/data/similarity_matrix.py</files>
  <read_first>
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Per-subdir loader module map" — `similarity_matrix.py` exposes `load()` (no country arg) returning the full matrix; "views slice".
    - `.planning/phases/01-foundation-data-layer/COLUMN_DISCOVERY.md` — section for `similarity_matrix/`. Likely a square matrix CSV with ISO3 codes both as row index and column headers.
    - `src/mosaic_dashboard/data/enso.py` (created in Plan 03) — no-country-arg loader template. Mirror its shape.
    - `src/mosaic_dashboard/data/who.py` (created in Plan 03) — for the `@st.cache_data` mtime pattern.
  </read_first>
  <action>
    Implement `src/mosaic_dashboard/data/similarity_matrix.py` with a single loader taking no country argument (the view does the row/column slice per RESEARCH.md §"Per-subdir loader module map"):

    Public function:
    - `load() -> pd.DataFrame` — Resolve root → check `<root>/similarity_matrix/` exists (warn + empty if not) → list CSVs (return empty if none) → take canonical file (sorted glob first, or named file per COLUMN_DISCOVERY) → call cached reader → return.
    - No country filter step (LAYER-10's Phase 5 view does `df.loc[country]` or `df[country]` to slice).

    Private cached reader:
    - `_read_similarity_matrix_cached(path: str, mtime: float) -> pd.DataFrame` with `@st.cache_data(show_spinner=False)`.
    - Reads the CSV. If COLUMN_DISCOVERY.md found that the first column is the ISO3 row index, use `pd.read_csv(path, index_col=0)` so the matrix is loaded with ISO3 as both index and columns. If discovery found a different shape (e.g., long-form `(row_iso3, col_iso3, value)`), use the appropriate read pattern and document it.
    - `require_columns` check is shape-dependent — for a square matrix with ISO3 column headers, the `REQUIRED_COLUMNS` set contains "at least one ISO3 column", which is hard to literal-encode. **Phase 1 simplification:** validate that the DataFrame has at least one row AND that the row index (or first column post-read) contains all-3-letter-uppercase string values. If `REQUIRED_COLUMNS` is set to an empty `frozenset()` (allowed by `require_columns`), the schema check passes trivially — combine this with an explicit defensive check inside `_read_*_cached`:
      ```
      if df.empty or not all(isinstance(c, str) and len(c) == 3 and c.isalpha() and c.isupper() for c in df.columns[:1]):
          # If matrix is degenerate, fall through to upstream — Phase 5 hardens
          pass
      ```
      Or, more cleanly, set `SIMILARITY_MATRIX_REQUIRED_COLUMNS = frozenset()` (no required columns — matrix shape is the contract, not column-name presence), and add a `# TODO(Phase 5): tighten validation once matrix dimensions are stable upstream` comment.

    Private empty factory:
    - `_empty() -> pd.DataFrame` returning `pd.DataFrame()` (zero rows, zero columns — a true empty matrix). This is fine for Phase 5's view which checks `df.empty` and renders an empty-state.
  </action>
  **Decisions addressed:** D-05 (one `similarity_matrix.py` module per `similarity_matrix/` subdir with single named `load()` fn — the no-country variant of the per-subdir loader rule), D-06 (loader-owned read shape via `index_col=0`), D-07 (ISO3 row/column index — canonical country identifier even in matrix form), D-10 (missing subdir → empty matrix + warning), D-12 (Phase 5 will tighten via `require_columns` once matrix shape stabilizes upstream), D-18 (`@st.cache_data` mtime-keyed), D-19 (plain `pd.read_csv` — no parquet/polars/duckdb), D-20 (public/private split lets Phase 5 swap matrix representation without changing `load()` signature) — see CONTEXT.md.
  <verify>
    <automated>uv run python -c "
import inspect
import pandas as pd
import streamlit as st
from mosaic_dashboard.data import similarity_matrix

class FakeSessionState(dict):
    def get(self, k, default=None): return super().get(k, default)
st.session_state = FakeSessionState()

# Signature: load() takes no country arg
fn = similarity_matrix.load
sig = inspect.signature(fn)
assert 'country' not in sig.parameters, 'similarity_matrix.load must NOT take country (full-matrix loader)'

# Cached reader exists
cached_fns = [v for k, v in vars(similarity_matrix).items() if k.startswith('_read_') and k.endswith('_cached')]
assert len(cached_fns) == 1, f'expected exactly 1 _read_*_cached, found {len(cached_fns)}'
assert hasattr(cached_fns[0], 'clear'), 'cached fn not @st.cache_data wrapped'

# Missing-subdir empty-state
import tempfile
with tempfile.TemporaryDirectory() as tmp:
    st.session_state['data_root_override'] = tmp
    df = similarity_matrix.load()
    assert isinstance(df, pd.DataFrame), f'expected DataFrame, got {type(df)}'
    assert df.empty, f'expected empty DF, got shape={df.shape}'

print('TASK3 OK')
"
# DATA-03: no network-library imports in the data layer (fails the verify if any are present)
if grep -rE '^(from |import )(requests|httpx|urllib\.request|aiohttp)\b' src/mosaic_dashboard/data/; then
  echo "DATA-03 FAIL: forbidden network import found in src/mosaic_dashboard/data/" >&2
  exit 1
fi
echo "DATA-03 OK: no network imports in src/mosaic_dashboard/data/"</automated>
  </verify>
  <acceptance_criteria>
    - `src/mosaic_dashboard/data/similarity_matrix.py` exports `load() -> pd.DataFrame` taking NO `country` argument
    - With `resolve_data_root()` pointing at an empty tempdir, `load()` returns an empty `pd.DataFrame` (no exception, no warning suppression failure)
    - The module has exactly one `_read_*_cached` function decorated with `@st.cache_data` (verified by `.clear()` method)
    - If COLUMN_DISCOVERY.md found a real similarity_matrix CSV, `load()` against the real `~/MOSAIC/MOSAIC-data/processed/` returns a non-empty DataFrame (manual sanity check during Plan 05 verification A1/A3)
    - The cached reader's signature is `(path: str, mtime: float)` per P7
    - No network-library imports in `src/mosaic_dashboard/data/` (grep returns no matches for `requests`/`httpx`/`urllib.request`/`aiohttp`) — DATA-03 enforced at the data-layer boundary
    - The verify command's assertions pass
  </acceptance_criteria>
  <done>Similarity-matrix loader (full-matrix, no country filter) ships with the canonical pattern. Phase 5 LAYER-10 view consumes it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → loader | Same as Plan 03: `pd.read_csv` and `Path.stat()` on a resolved root; no shell exec or symlink-escape exploit in scope. |
| filename glob → ISO3 list | `available_countries()` parses ISO3 prefixes from shapefile filenames; defensive 3-letter-uppercase check rejects malformed names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | upstream shapefile renamed to non-ISO3 pattern | mitigate | `available_countries()` defensively filters to 3-letter-uppercase prefixes; non-conforming filenames are skipped (silent), matching D-13's "tolerate irrelevant file additions". |
| T-01-11 | Information Disclosure | error messages echo subdir paths in warnings | accept | Single-user local stderr context; subdir paths are non-sensitive. |
</threat_model>

<verification>
After all three tasks:
1. `uv run python -c "from mosaic_dashboard.data import shapefiles, immunity, vaccine_effectiveness, symptomatic, similarity_matrix; print('imports ok')"` exits 0.
2. The three verify commands all print `TASK{N} OK`.
3. `grep -L 'geopandas\|import fiona\|import shapely' src/mosaic_dashboard/data/shapefiles.py` confirms no heavy geo deps imported.
4. Each loader's `_read_*_cached` symbol has a `.clear()` method (confirming `@st.cache_data` wrapping).
5. `shapefiles.available_countries()` against real `~/MOSAIC/MOSAIC-data/processed/shapefiles/` returns ~50+ ISO3 codes (the SSA + adjacent country set shipped upstream).
</verification>

<success_criteria>
- DATA-02 / DATA-03 / DATA-04 satisfied identically to Plan 03's big-five loaders, but for the small/static layers.
- API contract for the small-five matches RESEARCH.md §"Per-subdir loader module map" exactly — Phase 5 cleanly extends each loader's `_read_*_cached` body without touching public signatures (D-20 / D-09 honored).
- No geopandas / fiona / shapely / pyproj dependency added in Phase 1 (verified by grep). Phase 2 will add geopandas when the SSA map renders.
- `shapefiles.available_countries()` is the integration point for Phase 2's country picker AND for Plan 05's Data Status (validating the shapefiles/ subdir is populated).
- Plan 03 and Plan 04 share zero file modifications — they can execute in parallel as Wave 3.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-layer/01-04-SUMMARY.md` documenting:
- Final public signatures of every small-five loader (with/without `country` arg per spec)
- Whether each loader is full-impl (discovery found real files) or stub-with-discovery (subdir present but Phase 5 fleshes out); list the TODO(Phase 5) markers placed
- Real-data smoke test: `shapefiles.available_countries()` count and a sample of ISO3 codes returned against `~/MOSAIC/MOSAIC-data/processed/`
- Confirmation that no geopandas / fiona / shapely is imported anywhere in the data subpackage
- A heads-up to Plan 05: how to compose Data Status's per-subdir presence/file-count/mtime triplet — by `Path.stat()` directly OR by calling each loader and checking `df.empty`
</output>
</content>
</invoke>