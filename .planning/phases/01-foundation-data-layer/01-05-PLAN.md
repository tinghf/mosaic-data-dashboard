---
phase: 01-foundation-data-layer
plan: 05
type: execute
wave: 4
depends_on: [03, 04]
files_modified:
  - src/mosaic_dashboard/ui/sidebar.py
  - src/mosaic_dashboard/app.py
  - src/mosaic_dashboard/pages/__init__.py
  - src/mosaic_dashboard/pages/00_Data_Status.py
  - .planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md
autonomous: false
requirements: [DATA-01, DATA-02, DATA-04, ENV-02]
tags: [ui, streamlit, pages, data-status, acceptance]

must_haves:
  truths:
    - "D-17: `uv run streamlit run src/mosaic_dashboard/app.py` launches the dashboard with no errors"
    - "Visiting the entrypoint URL shows the welcome screen with the sidebar visible"
    - "D-11 + D-15: The sidebar contains a 'Data Status' link (Streamlit-native `pages/00_Data_Status.py`) that routes to `/Data_Status`"
    - "D-11: The Data Status page renders a table with one row per expected `processed/` subdir, showing presence + file count + latest mtime"
    - "D-03: The sidebar exposes an ephemeral data-root override text input; editing it changes the resolved path on the next interaction without persisting to config.toml"
    - "When `<root>/WHO/` is missing/renamed, the Data Status page shows WHO row as MISSING and the dashboard does NOT crash"
    - "Edits to `config.toml`'s `[data] root` take effect on the next page interaction"
  artifacts:
    - path: "src/mosaic_dashboard/ui/sidebar.py"
      provides: "render() — shared sidebar called from app.py and every page"
      contains: "def render"
    - path: "src/mosaic_dashboard/app.py"
      provides: "Streamlit entrypoint; calls set_page_config + logging.configure + sidebar.render + welcome screen"
      contains: "st.set_page_config"
    - path: "src/mosaic_dashboard/pages/00_Data_Status.py"
      provides: "Data Status page enumerating all 12 processed/ subdir entries (WHO split into 3 granularities + 9 others)"
      contains: "import streamlit as st"
    - path: ".planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md"
      provides: "Captured A1-A10 acceptance results per RESEARCH.md Validation Architecture"
  key_links:
    - from: "src/mosaic_dashboard/app.py"
      to: "src/mosaic_dashboard/ui/sidebar.py:render()"
      via: "Direct call as first non-set_page_config action"
      pattern: "render_sidebar|sidebar\\.render"
    - from: "src/mosaic_dashboard/pages/00_Data_Status.py"
      to: "src/mosaic_dashboard/ui/sidebar.py:render()"
      via: "Direct call at top of page per RESEARCH.md sidebar-on-every-page rule"
      pattern: "render_sidebar|sidebar\\.render"
    - from: "src/mosaic_dashboard/pages/00_Data_Status.py"
      to: "src/mosaic_dashboard/config.py:resolve_data_root()"
      via: "Page resolves the data root once and uses it to stat each subdir"
      pattern: "resolve_data_root"
    - from: "src/mosaic_dashboard/pages/00_Data_Status.py"
      to: "10 processed/ subdirs"
      via: "Each subdir gets a row showing presence/file count/latest mtime"
      pattern: "WHO|WASH|ENSO|demographics|OAG|shapefiles|immunity|vaccine_effectiveness|symptomatic|similarity_matrix"
---

<objective>
Land the visible Phase 1 surface: a shared sidebar helper with ephemeral data-root override, the Streamlit entrypoint, and the Data Status page that enumerates all expected `processed/` subdirs with presence + file count + latest mtime. Then run the A1-A10 acceptance checklist from RESEARCH.md §"Validation Architecture" and capture results in `01-05-ACCEPTANCE.md`. After this plan, all four ROADMAP §Phase 1 success criteria are verifiable end-to-end.

Purpose: Satisfy DATA-01 (sidebar override exposes the configurable path), the DATA-02 user-visible half (mtime column updates after a CSV edit), the DATA-04 user-visible half (missing-subdir row shows MISSING without crashing), and ENV-02 (one-liner launch verified end-to-end). The autonomous portion (build the three modules) completes first; the checkpoint at the end runs A1-A10 acceptance with a human.

Output: A runnable dashboard whose only visible view is the Data Status page, plus a captured acceptance log proving the phase ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-02-SUMMARY.md
@.planning/phases/01-foundation-data-layer/01-03-SUMMARY.md
@.planning/phases/01-foundation-data-layer/01-04-SUMMARY.md
@src/mosaic_dashboard/config.py
@src/mosaic_dashboard/logging_config.py
@src/mosaic_dashboard/data/shapefiles.py

<interfaces>
Contracts this plan consumes from Plans 02-04:

- `mosaic_dashboard.config.resolve_data_root() -> Path`
- `mosaic_dashboard.config.SESSION_KEY: str` (value `"data_root_override"`)
- `mosaic_dashboard.config.CONFIG_TOML_PATH: Path`
- `mosaic_dashboard.logging_config.configure(level: str) -> None` (idempotent)
- `mosaic_dashboard.data.shapefiles.available_countries() -> list[str]`

Expected subdirs Data Status must enumerate (from CONTEXT.md canonical_refs, WHO broken into 3 granularities):

`WHO/annual`, `WHO/weekly`, `WHO/daily`, `WASH`, `ENSO`, `demographics`, `OAG`, `shapefiles`, `immunity`, `vaccine_effectiveness`, `symptomatic`, `similarity_matrix` — 12 rows total.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement shared sidebar and entrypoint</name>
  <files>src/mosaic_dashboard/ui/sidebar.py, src/mosaic_dashboard/app.py</files>
  <read_first>
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Configuration + Sidebar Override > Sidebar render helper (src/mosaic_dashboard/ui/sidebar.py)" — copy-pasteable skeleton.
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Configuration + Sidebar Override > app.py skeleton" — entrypoint skeleton.
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Configuration + Sidebar Override > Why a shared helper instead of an entrypoint widget" — explains why `render()` must be called on EVERY page.
    - `.planning/phases/01-foundation-data-layer/01-CONTEXT.md` (D-03 ephemeral override; D-15 pages/ directory; D-17 launch one-liner)
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Streamlit Multi-Page Conventions" — `set_page_config` must be the very first Streamlit call.
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Pitfalls" P9 (`render()` initializes the SESSION_KEY slot).
  </read_first>
  <action>
    Implement `src/mosaic_dashboard/ui/sidebar.py` per RESEARCH.md §"Sidebar render helper" skeleton. Module exports a single public `render() -> None` function.

    Required behaviors:
    - Import `SESSION_KEY` from `mosaic_dashboard.config` — do NOT hardcode the string literal `"data_root_override"` in this file. Single source of truth lives in `config.py` (Plan 02).
    - Renders `st.sidebar.header("Mosaic Dashboard")` (or the project name from PROJECT.md — keep terse).
    - Initialize `st.session_state[SESSION_KEY] = ""` if the key is absent, BEFORE rendering the widget (P9 mitigation: prevents KeyError when a page reads the slot before render).
    - Render the data-root override widget via `st.sidebar.text_input(label, key=SESSION_KEY, placeholder=..., help=...)`. The `key=SESSION_KEY` binding lets Streamlit auto-sync widget value to session_state.
    - Placeholder text: `"Leave blank to use config.toml or default (~/MOSAIC/MOSAIC-data/processed)"`.
    - Help tooltip: `"Ephemeral override for this browser session. Does NOT persist to config.toml. Resolution order: this field → config.toml → default."`.
    - NO `@st.cache_data` decorator — UI re-renders on every script run.
    - `from __future__ import annotations` at the top.

    Implement `src/mosaic_dashboard/app.py` per RESEARCH.md §"app.py skeleton". Required order of top-level calls:

    1. `import streamlit as st`
    2. `st.set_page_config(page_title="Mosaic Data Dashboard", layout="wide", initial_sidebar_state="expanded")` — MUST be the first Streamlit call per RESEARCH.md §"set_page_config interaction". Omit `page_icon` per the project's no-emojis policy.
    3. Read `[logging] level` from `CONFIG_TOML_PATH` (use `tomllib.load` in binary mode; fall back to `"INFO"` if file missing or key absent).
    4. Call `configure_logging(log_level)` from `mosaic_dashboard.logging_config`.
    5. Call `render_sidebar()` from `mosaic_dashboard.ui.sidebar`.
    6. Render the welcome body: `st.title("Mosaic Data Dashboard")` + a one-paragraph `st.write(...)` directing the user to Data Status in the sidebar.

    NO `if __name__ == "__main__":` guard — Streamlit imports the script directly per RESEARCH.md §"Streamlit Multi-Page Conventions"; top-level code runs on every rerun.

    Imports use ABSOLUTE package paths (`from mosaic_dashboard.ui.sidebar import ...`), not relative (per RESEARCH.md gotcha 4 — pages run as scripts not modules; same convention extended to app.py for consistency).
  </action>
  **Decisions addressed:** D-03 (sidebar exposes ephemeral text-input override for the data-root path — session-scoped, does NOT persist to `config.toml`), D-17 (`app.py` entry path matches the documented launch one-liner `uv sync && uv run streamlit run src/mosaic_dashboard/app.py`; this task is the runtime end of that contract) — see CONTEXT.md.
  <verify>
    <automated>uv run python -c "
import inspect
from mosaic_dashboard.ui import sidebar

# render() exists and takes no args
fn = sidebar.render
sig = inspect.signature(fn)
assert len(sig.parameters) == 0, f'render() must take no args, got {sig}'

# Imports SESSION_KEY (not hardcoded literal)
src = inspect.getsource(sidebar)
assert 'from mosaic_dashboard.config' in src and 'SESSION_KEY' in src, 'sidebar.py must import SESSION_KEY from mosaic_dashboard.config'
# Hardcoded literal check
literal = 'data_root_override'
literal_quoted = '\"' + literal + '\"'
literal_squoted = \"'\" + literal + \"'\"
assert literal_quoted not in src and literal_squoted not in src, 'sidebar.py must NOT hardcode the SESSION_KEY string literal'

# app.py exists and has the required structure
with open('src/mosaic_dashboard/app.py') as f:
    app_src = f.read()
assert 'st.set_page_config' in app_src, 'app.py must call st.set_page_config'
assert 'render' in app_src, 'app.py must call sidebar render'
assert 'configure' in app_src, 'app.py must call logging_config.configure'
assert 'tomllib' in app_src, 'app.py must read [logging] level from config.toml via tomllib'

import py_compile
py_compile.compile('src/mosaic_dashboard/app.py', doraise=True)
py_compile.compile('src/mosaic_dashboard/ui/sidebar.py', doraise=True)
print('TASK1_STATIC_OK')
" &amp;&amp; (uv run streamlit run src/mosaic_dashboard/app.py --server.headless true --server.port 8765 --server.runOnSave false &amp; APPPID=$!; sleep 8; HTTP_CODE=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:8765/); kill $APPPID 2>/dev/null; wait $APPPID 2>/dev/null; if [ "$HTTP_CODE" = "200" ]; then echo 'STREAMLIT_LAUNCH_OK'; else echo "STREAMLIT_LAUNCH_FAIL code=$HTTP_CODE"; fi; true)</automated>
  </verify>
  <acceptance_criteria>
    - `src/mosaic_dashboard/ui/sidebar.py` defines `render() -> None` taking no arguments
    - `ui/sidebar.py` imports `SESSION_KEY` from `mosaic_dashboard.config` (not a hardcoded string literal)
    - `render()` initializes `st.session_state[SESSION_KEY] = ""` if the key is absent
    - `render()` calls `st.sidebar.text_input(..., key=SESSION_KEY, ...)` binding the widget to session state
    - `src/mosaic_dashboard/app.py` calls `st.set_page_config(...)` as the FIRST Streamlit call
    - `src/mosaic_dashboard/app.py` calls `configure_logging` and `render_sidebar` exactly once each, after `set_page_config`
    - `src/mosaic_dashboard/app.py` reads `[logging] level` from `config.toml` via `tomllib`
    - `app.py` does NOT contain any emoji characters
    - `uv run streamlit run src/mosaic_dashboard/app.py --server.headless true --server.port 8765` responds with HTTP 200 on the root URL within 10 seconds (verify output line `STREAMLIT_LAUNCH_OK`)
  </acceptance_criteria>
  <done>Sidebar helper + entrypoint shipped; dashboard launches; ENV-02 launchability proven end-to-end via headless smoke check.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the Data Status page</name>
  <files>src/mosaic_dashboard/pages/__init__.py, src/mosaic_dashboard/pages/00_Data_Status.py</files>
  <read_first>
    - `.planning/phases/01-foundation-data-layer/01-CONTEXT.md` (D-11 Data Status enumerates expected subdirs with presence + file count + most-recent mtime; D-15 `pages/00_Data_Status.py` zero-padded prefix)
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Streamlit Multi-Page Conventions" — pages/ filename rules, `00_Data_Status.py` → sidebar label "Data Status", URL `/Data_Status`; "Page files run as scripts, not modules. Do NOT use relative imports inside pages/."
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Logging & Error Reporting > logging.warning vs st.warning" — Data Status is the ONE place that translates layer state into UI via `st.warning`/`st.error`.
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Pitfalls" P1 (two-digit zero-padded prefix) and P9 (call `render_sidebar()` first).
    - `.planning/phases/01-foundation-data-layer/01-04-SUMMARY.md` — guidance on composing Data Status from Path.stat directly vs loader metadata helpers.
  </read_first>
  <action>
    Create `src/mosaic_dashboard/pages/__init__.py` as an empty package marker (one-line module docstring is fine). Note: per RESEARCH.md gotcha 4, page files use ABSOLUTE imports (`from mosaic_dashboard.config import ...`) not relative.

    Implement `src/mosaic_dashboard/pages/00_Data_Status.py` with strict top-of-file order:

    1. `import streamlit as st`
    2. `st.set_page_config(page_title="Data Status", layout="wide")` — per-page override is allowed; sets browser tab title.
    3. `from mosaic_dashboard.ui.sidebar import render as render_sidebar` then call `render_sidebar()` — MUST run on every page per RESEARCH.md §5 (keeps the override widget alive across navigation, P9 mitigation).
    4. Remaining imports: `from pathlib import Path`, `import pandas as pd`, `from datetime import datetime`, `from mosaic_dashboard.config import resolve_data_root`, `from mosaic_dashboard.data import shapefiles`.

    Module-level constant — single source of truth for what Data Status enumerates:
    ```
    EXPECTED_SUBDIRS = [
        "WHO/annual", "WHO/weekly", "WHO/daily",
        "WASH", "ENSO", "demographics", "OAG", "shapefiles",
        "immunity", "vaccine_effectiveness", "symptomatic", "similarity_matrix",
    ]
    ```

    Helper function (module-level, NOT cached because mtime must be fresh per DATA-02):
    - `_subdir_status(root: Path, subdir_rel: str) -> dict[str, Any]` — returns a row dict with keys `subdir` (str: the rel path passed in), `status` (str: one of "OK", "EMPTY", "MISSING", "INVALID"), `file_count` (int), `latest_mtime` (str: ISO-8601 or "—"), and `latest_mtime_epoch` (float | None: for sorting).
      - If `(root / subdir_rel)` does not exist: status="MISSING", file_count=0, mtime="—".
      - If exists but `.is_dir()` is False (e.g., upstream replaced with a file): status="INVALID", file_count=0, mtime="—".
      - If exists and is a dir: list direct children that are regular files (`p.iterdir()` then filter `f.is_file()`). file_count = count. If 0, status="EMPTY", mtime="—". Else status="OK", mtime = max `f.stat().st_mtime` rendered via `datetime.fromtimestamp(...).isoformat(timespec="seconds")`.

    Page body:
    - `st.title("Data Status")` and a brief `st.write(...)`: "Verify that the MOSAIC-data/processed/ checkout is hooked up correctly. Each row is one expected subdirectory."
    - Resolve the data root: `root = resolve_data_root()`.
    - Render the resolved path: `st.caption(f"Resolved data root: \`{root}\`")` so the user can see what's being inspected.
    - If `not root.exists()`: render `st.error(f"Data root not found: {root}. Edit config.toml or use the sidebar override.")` and `st.stop()` (per A7).
    - Build the table: `rows = [_subdir_status(root, s) for s in EXPECTED_SUBDIRS]`; `df = pd.DataFrame(rows)`.
    - Render: `st.dataframe(df[["subdir", "status", "file_count", "latest_mtime"]], use_container_width=True, hide_index=True)`.
    - Per-status summary banners (per RESEARCH.md §"logging.warning vs st.warning" — Data Status is where layer state translates to UI):
      - If any row has status=="MISSING": `st.warning(f"Missing subdirs: {', '.join(...)}. Loaders for these layers will return empty DataFrames.")`.
      - If any row has status=="EMPTY": `st.info(f"Empty subdirs: {', '.join(...)}. Layer is present but contains no files.")`.
      - If any row has status=="INVALID": `st.error(f"Invalid path (not a directory): {', '.join(...)}.")`.
    - Shapefile integration metric beneath the table:
      ```
      st.metric("Countries with shapefiles", len(shapefiles.available_countries()))
      ```
      (Proves Plan 04's `available_countries()` is wired in; Phase 2 will reuse it for the country picker.)
  </action>
  **Decisions addressed:** D-11 (Phase 1 ships a dedicated "Data Status" Streamlit page enumerating each expected `processed/` subdir with presence, file count, and most-recent mtime — the primary view of Phase 1), D-15 (page lives at `pages/00_Data_Status.py` using the Streamlit-native `pages/` directory with zero-padded `00_` prefix so it's first in the sidebar) — see CONTEXT.md.
  <verify>
    <automated>python -c "
import py_compile
py_compile.compile('src/mosaic_dashboard/pages/00_Data_Status.py', doraise=True)
print('compile OK')
" &amp;&amp; test -f src/mosaic_dashboard/pages/00_Data_Status.py &amp;&amp; test -f src/mosaic_dashboard/pages/__init__.py &amp;&amp; grep -q 'EXPECTED_SUBDIRS' src/mosaic_dashboard/pages/00_Data_Status.py &amp;&amp; grep -q 'WHO/annual' src/mosaic_dashboard/pages/00_Data_Status.py &amp;&amp; grep -q 'similarity_matrix' src/mosaic_dashboard/pages/00_Data_Status.py &amp;&amp; grep -q 'set_page_config' src/mosaic_dashboard/pages/00_Data_Status.py &amp;&amp; grep -qE 'render_sidebar|sidebar\.render' src/mosaic_dashboard/pages/00_Data_Status.py &amp;&amp; grep -q 'resolve_data_root' src/mosaic_dashboard/pages/00_Data_Status.py &amp;&amp; grep -q 'available_countries' src/mosaic_dashboard/pages/00_Data_Status.py &amp;&amp; (uv run streamlit run src/mosaic_dashboard/app.py --server.headless true --server.port 8766 --server.runOnSave false &amp; APPPID=$!; sleep 8; HTTP_CODE=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:8766/Data_Status); kill $APPPID 2>/dev/null; wait $APPPID 2>/dev/null; if [ "$HTTP_CODE" = "200" ]; then echo 'DATA_STATUS_PAGE_OK'; else echo "DATA_STATUS_PAGE_FAIL code=$HTTP_CODE"; fi; true)</automated>
  </verify>
  <acceptance_criteria>
    - `src/mosaic_dashboard/pages/00_Data_Status.py` exists with the exact filename (two-digit zero-padded prefix `00_` per P1)
    - `src/mosaic_dashboard/pages/__init__.py` exists
    - The page calls `st.set_page_config` then `render_sidebar()` before any other content
    - Page defines `EXPECTED_SUBDIRS` constant with 12 entries (3 WHO granularities + 9 others)
    - Page references all 10 base subdir names (`WHO`, `WASH`, `ENSO`, `demographics`, `OAG`, `shapefiles`, `immunity`, `vaccine_effectiveness`, `symptomatic`, `similarity_matrix`) — verified via grep
    - Page calls `resolve_data_root()` and `shapefiles.available_countries()`
    - Streamlit launches and the route `/Data_Status` responds with HTTP 200 (verify line `DATA_STATUS_PAGE_OK`)
    - Module compiles cleanly via `py_compile`
  </acceptance_criteria>
  <done>Data Status page renders all 12 expected subdir rows with presence/file-count/mtime; route `/Data_Status` is reachable via the auto-discovered sidebar; the page satisfies D-11 and the DATA-04 user-visible empty-state requirement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: A1-A10 acceptance check (human verification)</name>
  <files>.planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md</files>
  <read_first>
    - `.planning/phases/01-foundation-data-layer/01-RESEARCH.md` §"Validation Architecture" — full A1-A10 acceptance table (each row maps to a Phase 1 requirement and an exact verification step).
    - `.planning/ROADMAP.md` §"Phase 1: Foundation & Data Layer" — the 4 user-facing success criteria.
  </read_first>
  <action>
    This is a human-verify checkpoint. Run the A1-A10 acceptance checks listed in the &lt;how-to-verify&gt; block below and capture results in `.planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md`. No autonomous code changes — the executor pauses here for the user to perform the manual checks and report `approved` or `gaps: &lt;list&gt;`.
  </action>
  **Decisions addressed:** This checkpoint validates the user-visible side of D-03 (A7 — sidebar override changes effective path mid-session), D-11 (A3 — Data Status page renders all 12 expected-subdir rows), D-17 (A2 + A10 — the documented launch one-liner actually launches the dashboard, and README contains it verbatim) — see CONTEXT.md.
  <verify>
    <automated>test -f .planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md && awk -v c=0 "/^## A/{c++} END{ if (c &gt;= 10) print \"ACCEPTANCE_LOG_COMPLETE\"; else print \"ACCEPTANCE_LOG_INCOMPLETE c=\"c }" .planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md && grep -E "^## Phase 1 status: (READY TO TRANSITION|GAPS PRESENT)" .planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md</automated>
  </verify>
  <what-built>
    All five plans in Phase 1 have shipped:
    - Plan 01: uv scaffold, pyproject.toml, uv.lock, config.toml, README, CLAUDE.md, COLUMN_DISCOVERY.md
    - Plan 02: errors.py, _schema.py, logging_config.py, config.py
    - Plan 03: big-five loaders (who, wash, enso, demographics, oag)
    - Plan 04: small-five loaders (shapefiles, immunity, vaccine_effectiveness, symptomatic, similarity_matrix)
    - Plan 05 Tasks 1-2: ui/sidebar.py, app.py, pages/00_Data_Status.py

    The dashboard runs via `uv sync && uv run streamlit run src/mosaic_dashboard/app.py`; the Data Status page enumerates 12 expected subdirs.
  </what-built>
  <how-to-verify>
    Execute the A1-A10 acceptance checks from RESEARCH.md §"Validation Architecture" and record results in `.planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md`. Use this template per check: `## A{N}: <criterion> — PASS | FAIL | N/A`, followed by what was observed and (if FAIL) the gap to close.

    The ten checks (verbatim from RESEARCH.md §7):

    1. **A1 — `uv sync` succeeds from a fresh clone:** `cd $(mktemp -d) && git clone <repo> . && uv sync` exits 0; `.venv/` and `uv.lock` exist.
    2. **A2 — `uv run streamlit run src/mosaic_dashboard/app.py` launches:** Streamlit prints "You can now view your Streamlit app in your browser." and serves on port 8501.
    3. **A3 — Data Status page reachable and renders:** Click "Data Status" in sidebar → page renders a table with all 12 EXPECTED_SUBDIRS rows showing presence/file-count/mtime.
    4. **A4 — Offline: dashboard still loads:** Disconnect network (e.g., `sudo ip link set <iface> down` or disable wifi) → reload localhost:8501 → app still renders, Data Status still works.
    5. **A5 — Missing subdir produces empty DF + warning, no crash:** Temporarily rename `~/MOSAIC/MOSAIC-data/processed/WHO/` to `WHO.bak/` → reload Data Status → WHO/annual + WHO/weekly + WHO/daily rows show MISSING; `logging.warning` line appears in the terminal where `streamlit run` is executing; no traceback in the browser. RESTORE the rename.
    6. **A6 — Malformed CSV produces SchemaMismatchError:** Make a copy of a WHO CSV with a required column dropped (write to a tempdir). Point the sidebar override at that tempdir's parent. Reload. Either: (a) call the loader from `uv run python -c "from mosaic_dashboard.data import who; who.load_annual('AGO')"` after setting `MOSAIC_DATA_PATH` (or after a sidebar override is set in Streamlit context), OR (b) confirm via a brief inline script that `SchemaMismatchError` is raised with the dataset name and missing-column set.
    7. **A7 — Sidebar override changes effective path mid-session:** Type a valid alternate path in the sidebar text input → next interaction shows Data Status reflecting that path's contents.
    8. **A8 — CSV edit shows on next page interaction without restart:** Edit one of the WHO CSVs (add or remove a benign row) → reload Data Status → the mtime column updates to the new timestamp.
    9. **A9 — `uv.lock` is committed:** `git ls-files | grep uv.lock` returns a hit.
    10. **A10 — README documents the launch one-liner:** `grep -F 'uv sync && uv run streamlit run src/mosaic_dashboard/app.py' README.md` returns a hit.

    If any check FAILs, do NOT close the phase. Note the gap; the orchestrator may invoke `/gsd-plan-phase --gaps` to address it.

    Write the captured results to `.planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md` with one section per check. Mark overall phase status at the bottom: `## Phase 1 status: READY TO TRANSITION` (if all 10 pass or are N/A with rationale) or `## Phase 1 status: GAPS PRESENT` with a list of failed checks.

    For A5 specifically: a CLI alternative if you don't want to physically rename the upstream dir is to set `MOSAIC_DATA_PATH` to a sibling tempdir (e.g., via the sidebar override) where some subdirs exist and some don't. That achieves the same observable outcome.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` — all 10 checks recorded, overall PASS (or PASS with N/A rationales). Phase 1 ready to transition.
    - `gaps: <list>` — describe failures and which checks failed. The orchestrator may then invoke `/gsd-plan-phase 01 --gaps` to close them.
  </resume-signal>
  <acceptance_criteria>
    - `.planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md` exists
    - File contains a section for each of A1 through A10
    - Each section is marked PASS, FAIL, or N/A (with rationale for N/A)
    - File ends with `## Phase 1 status: READY TO TRANSITION` OR `## Phase 1 status: GAPS PRESENT` with explicit gap list
    - If status is READY TO TRANSITION, every locked requirement (DATA-01, DATA-02, DATA-03, DATA-04, ENV-01, ENV-02) is satisfied by at least one passing check (the A-row's "Maps to" column makes this explicit)
  </acceptance_criteria>
  <done>Human-verified A1-A10 acceptance log captured. Phase 1 either ships clean or its gap closure work is scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Streamlit | Local-only single-user dashboard on 127.0.0.1; no auth (PROJECT.md out-of-scope). |
| sidebar widget → session_state → resolve_data_root | User-typed path flows into `Path(...).expanduser()`; single-user local context, no exploit surface. |
| Data Status → filesystem | `Path.stat()` and `Path.iterdir()` on the resolved root; no writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Tampering | Data Status accidentally writes to upstream | mitigate | Page only calls `Path.stat`/`Path.iterdir` and reads via cached loaders; no `Path.write_*` calls; CLAUDE.md (Plan 01) codifies read-only constraint for future agents. |
| T-01-13 | DoS | `iterdir()` over a directory with thousands of files | accept | `processed/` subdirs ship with a small number of files (per ls inspection of upstream); if Phase 5+ shows hot subdirs, swap to `os.scandir` or limit listing. |
| T-01-14 | Information Disclosure | Sidebar widget echoes the user's home path | accept | Single-user local browser; path is not transmitted off-machine. |
</threat_model>

<verification>
After all three tasks:
1. `uv run streamlit run src/mosaic_dashboard/app.py --server.headless true --server.port 8765` launches and `/` and `/Data_Status` both return HTTP 200.
2. The Data Status table shows 12 rows; for a fully-populated `~/MOSAIC/MOSAIC-data/processed/`, all 12 are status="OK".
3. `.planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md` exists and records all 10 A-checks.
4. A1-A10 collectively cover all six Phase 1 requirements (DATA-01..04, ENV-01, ENV-02) — verified by the "Maps to" column in RESEARCH.md §7.
</verification>

<success_criteria>
- DATA-01 (configurable data path) END-TO-END verified: sidebar override → config.toml → default fallback all observable in the running dashboard (A7).
- DATA-02 (fresh-read with invisible caching) END-TO-END verified: edit a CSV, mtime column updates on next interaction (A8).
- DATA-04 (lenient half: missing subdir → empty + warning, no crash) END-TO-END verified by A5; (strict half: schema mismatch → SchemaMismatchError) verified by A6.
- ENV-02 (one-liner launches dashboard) END-TO-END verified by A2 + A10 (README + Streamlit launches).
- ROADMAP.md §Phase 1 all four success criteria are observably true (each maps to one or more A-checks).
- The dashboard's only visible surface in Phase 1 is the Data Status page — Phases 2-7 layer additional pages on top without modifying the foundation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-layer/01-05-SUMMARY.md` documenting:
- Final shape of `ui/sidebar.py` (signature, session_state integration point)
- Final shape of `app.py` (logging boot, page_config, render_sidebar call order)
- Data Status page columns + the EXPECTED_SUBDIRS list (as the contract Phases 2+ may extend)
- Pointer to `01-05-ACCEPTANCE.md` for the A1-A10 results
- Any deviations from the RESEARCH.md skeletons (e.g., if a check required adjusting the page layout) and why
- Heads-up for Phase 2: the sidebar widget's `country_picker()` lands inside `ui/sidebar.py` and reuses the same session_state pattern (per RESEARCH.md §5 "Why this generalizes for Phase 2")
</output>
</content>
</invoke>