---
phase: 02-country-navigation-ssa-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/mosaic_dashboard/config.py
  - src/mosaic_dashboard/data/country_metadata.py
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - MAP-01
  - MAP-02
must_haves:
  truths:
    - "D-21 + D-22: uv add geopandas folium streamlit-folium succeeds and uv.lock is updated — Phase 2 IS the gate for the geo stack (geopandas is the geometry-reading library per D-22; folium + streamlit-folium is the map stack per D-21; Phase 1's D-19 'no geo in Phase 1' relaxes here)."
    - "D-24 + D-28: data/country_metadata.py exposes a hand-curated static ISO3→name lookup (D-24); minimal columns for v1 are iso3 + name only (D-28 — region/population/etc. deferred until a Phase needs them)."
    - "country_metadata.iter_countries() returns 54 (ISO3, name) tuples in alphabetical-by-name order with Angola first (D-25, D-26, D-27)"
    - "country_metadata.name_for(iso3) returns the display name for a known ISO3 and returns the iso3 itself for unknown codes (no raise)"
    - "country_metadata.ISO3_SET is the frozenset of all 54 valid ISO3 codes (used by the click-handler in 02-04 to guard against ESH/-99 leak)"
    - "config.COUNTRY_SESSION_KEY is defined as a module-level constant string mirroring SESSION_KEY (D-32)"
    - "country_metadata.warn_if_drifted_from_shapefiles(available_iso3s) logs a warning when the metadata set diverges from the shapefile set (D-29) and never raises"
  artifacts:
    - path: "pyproject.toml"
      provides: "Phase 2 dependencies added"
      contains: "geopandas"
    - path: "uv.lock"
      provides: "Lock file updated by uv add"
    - path: "src/mosaic_dashboard/data/country_metadata.py"
      provides: "Static ISO3->name table + iter_countries/name_for/ISO3_SET/warn_if_drifted_from_shapefiles"
      exports: ["COUNTRIES", "ISO3_SET", "iter_countries", "name_for", "warn_if_drifted_from_shapefiles"]
    - path: "src/mosaic_dashboard/config.py"
      provides: "COUNTRY_SESSION_KEY constant added"
      contains: "COUNTRY_SESSION_KEY"
  key_links:
    - from: "src/mosaic_dashboard/config.py"
      to: "src/mosaic_dashboard/data/country_metadata.py"
      via: "ISO3 alpha-3 contract"
      pattern: "COUNTRY_SESSION_KEY"
    - from: "src/mosaic_dashboard/data/country_metadata.py"
      to: "src/mosaic_dashboard/data/shapefiles.py"
      via: "ISO3 set comparison for D-29 drift warning"
      pattern: "warn_if_drifted_from_shapefiles"
---

<objective>
Land the Phase 2 scaffolding: add the three geo-stack dependencies (geopandas, folium, streamlit-folium) via `uv add`, create the static ISO3->name lookup module at `src/mosaic_dashboard/data/country_metadata.py`, and define `COUNTRY_SESSION_KEY` in `config.py`.

Purpose: Plans 02-02, 02-03, 02-04 all import from these artifacts. This is the single sequential prerequisite for the rest of Phase 2. Implements D-21..D-29 and D-32 (the constant).

Output:
- `pyproject.toml` + `uv.lock` updated by `uv add geopandas folium streamlit-folium`
- New module `src/mosaic_dashboard/data/country_metadata.py` with the 54-entry table + `iter_countries`, `name_for`, `ISO3_SET`, `warn_if_drifted_from_shapefiles`
- `COUNTRY_SESSION_KEY` constant in `config.py`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-country-navigation-ssa-map/02-CONTEXT.md
@.planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md
@.planning/phases/02-country-navigation-ssa-map/02-UI-SPEC.md
@CLAUDE.md
@pyproject.toml
@src/mosaic_dashboard/config.py
@src/mosaic_dashboard/data/shapefiles.py

<interfaces>
<!-- Pre-existing surface this plan extends. -->

From src/mosaic_dashboard/config.py (Phase 1, unchanged here except for the new constant):
```python
DEFAULT_DATA_ROOT: Path = Path("~/MOSAIC/MOSAIC-data/processed").expanduser()
SESSION_KEY: str = "data_root_override"
CONFIG_TOML_PATH: Path = ...
def resolve_data_root() -> Path: ...
```

From src/mosaic_dashboard/data/shapefiles.py (Phase 1, unchanged in 02-01):
```python
def available_countries() -> list[str]: ...   # returns 54 sorted ISO3 codes from XXX_ADM0.shp filenames
```

<!-- Interfaces this plan CREATES, consumed by 02-02 / 02-03 / 02-04. -->

In src/mosaic_dashboard/data/country_metadata.py (NEW):
```python
COUNTRIES: tuple[tuple[str, str], ...]   # ordered (iso3, display_name) pairs; 54 entries; D-25/D-27
ISO3_SET: frozenset[str]                 # set membership test for click-handler guard (D-29)

def iter_countries() -> list[tuple[str, str]]: ...   # D-29 public API
def name_for(iso3: str) -> str: ...                  # D-29 public API; never raises; returns iso3 for unknown codes
def warn_if_drifted_from_shapefiles(available_iso3s: Iterable[str]) -> None: ...   # D-29 startup sanity check
```

In src/mosaic_dashboard/config.py (NEW constant only):
```python
COUNTRY_SESSION_KEY: str = "selected_country_iso3"   # D-32 — single source of truth for selected ISO3
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 2 dependencies via uv add and define COUNTRY_SESSION_KEY in config.py</name>
  <files>pyproject.toml, uv.lock, src/mosaic_dashboard/config.py</files>
  <read_first>
    - .planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md §"Standard Stack" (versions + install command)
    - .planning/phases/02-country-navigation-ssa-map/02-CONTEXT.md D-22, D-32
    - CLAUDE.md §1 (uv run only) and §4 (uv add, never pip)
    - pyproject.toml (existing dependencies block)
    - src/mosaic_dashboard/config.py (existing SESSION_KEY pattern — the constant to mirror)
  </read_first>
  <action>
    Two sub-steps in a single task because both are tiny:

    (a) From the repo root run `uv add geopandas folium streamlit-folium` (RESEARCH §"Installation Command"). This atomically updates `[project.dependencies]` in `pyproject.toml` and rewrites `uv.lock`. Do NOT explicitly add pyogrio / shapely / pyproj / branca / xyzservices — they are transitive (RESEARCH §"Transitive Dependencies"). Do NOT add `fiona` — geopandas 1.x defaults to pyogrio (RESEARCH §"Transitive Dependencies"). After the command exits 0, verify with `git diff pyproject.toml` that exactly three new lines were added under `dependencies = [...]` (geopandas, folium, streamlit-folium) — if `uv` writes them with strict pins, accept the strict pin as-is; if you want the soft pin recommended in RESEARCH §"Version pins for pyproject.toml" (e.g. `"geopandas>=1.1,<2.0"`), edit `pyproject.toml` to match those pins AFTER the `uv add` step but BEFORE `uv sync`, then run `uv sync` to refresh the lock.

    (b) Edit `src/mosaic_dashboard/config.py`: add a new module-level constant `COUNTRY_SESSION_KEY: str = "selected_country_iso3"` immediately after the existing `SESSION_KEY` constant (per D-32 — co-located with Phase 1's `SESSION_KEY` for consistency, per RESEARCH §"Architectural Responsibility Map"). Include a docstring comment referencing D-32 and explaining that the constant is the single source of truth for the selected country across the entire app (selectbox writes to it, map-page click handler writes to it, every Phase 3+ view reads from it). The string value `"selected_country_iso3"` is intentionally descriptive (per "Claude's Discretion" in CONTEXT.md — implementer chooses the name; the spec only requires it be well-named). Do NOT change `SESSION_KEY` itself or `resolve_data_root()`.

    No changes to any other file in this task.
  </action>
  <verify>
    <automated>uv run python -c "from mosaic_dashboard.config import COUNTRY_SESSION_KEY; assert COUNTRY_SESSION_KEY == 'selected_country_iso3'; print('OK')" &amp;&amp; uv run python -c "import geopandas, folium, streamlit_folium; print(geopandas.__version__, folium.__version__, streamlit_folium.__version__)" &amp;&amp; grep -q "geopandas" pyproject.toml &amp;&amp; grep -q "folium" pyproject.toml &amp;&amp; grep -q "streamlit-folium" pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` `dependencies` list contains three new entries: one each starting `geopandas`, `folium`, `streamlit-folium`.
    - `uv.lock` is updated (modified in git working tree relative to HEAD).
    - `uv run python -c "import geopandas, folium, streamlit_folium"` exits 0.
    - `grep -E '^COUNTRY_SESSION_KEY[ :=]' src/mosaic_dashboard/config.py` returns exactly one match.
    - `uv run python -c "from mosaic_dashboard.config import COUNTRY_SESSION_KEY; print(COUNTRY_SESSION_KEY)"` prints `selected_country_iso3`.
    - Phase 1 imports still resolve: `uv run python -c "from mosaic_dashboard.config import SESSION_KEY, resolve_data_root, DEFAULT_DATA_ROOT; print('OK')"` exits 0.
  </acceptance_criteria>
  <done>
    `uv add` has produced a clean diff of pyproject.toml + uv.lock with the three Phase 2 deps; `COUNTRY_SESSION_KEY` is exported from `mosaic_dashboard.config`; existing Phase 1 imports (SESSION_KEY, resolve_data_root, DEFAULT_DATA_ROOT) remain intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create country_metadata.py with the 54-entry table and iter_countries/name_for/ISO3_SET/warn_if_drifted helpers</name>
  <files>src/mosaic_dashboard/data/country_metadata.py</files>
  <read_first>
    - .planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md §"country_metadata.py — Full Module File" (verbatim 54-entry table — copy the COUNTRIES tuple as-is)
    - .planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md §"CRITICAL FINDING: 54 ≠ 54" (why MUS/SYC are in metadata but NOT in AFRICA_ADM0; why ESH/-99 are in AFRICA_ADM0 but NOT in metadata)
    - .planning/phases/02-country-navigation-ssa-map/02-CONTEXT.md D-24, D-25, D-26, D-27, D-28, D-29
    - src/mosaic_dashboard/data/shapefiles.py (the `available_countries()` function — its return is the input to `warn_if_drifted_from_shapefiles`)
    - src/mosaic_dashboard/data/__init__.py (existing module docstring; this new module sits alongside the per-subdir loaders but is NOT a per-subdir loader — it's a SIDE module per CONTEXT.md "Established Patterns")
  </read_first>
  <action>
    Create `src/mosaic_dashboard/data/country_metadata.py` (a brand-new module under the `data/` package). Copy the COUNTRIES tuple verbatim from RESEARCH.md §"country_metadata.py — Full Module File" — all 54 entries in alphabetical-by-name order with Angola first (D-25, D-26, D-27). The names follow MOSAIC project convention with the 8 expansions documented in RESEARCH (e.g. "Democratic Republic of the Congo" not "Dem. Rep. Congo", "Eswatini" not "eSwatini", "São Tomé and Príncipe" with full diacritics).

    Module structure (per RESEARCH skeleton + D-29 API):
    - Module docstring referencing D-24..D-29 — explains this module is intentionally NOT generated from AFRICA_ADM0.dbf (MOSAIC-conventional names win over Natural Earth names), and that **order matters** (per D-25, the tuple order IS the picker display order, and per D-26 the first entry IS the first-load default).
    - `from __future__ import annotations`
    - `import logging` and `from typing import Iterable`
    - `log = logging.getLogger(__name__)`
    - `COUNTRIES: tuple[tuple[str, str], ...]` — the 54-entry tuple. Each line carries an inline comment for the 8 names edited from the DBF (per RESEARCH §"Strategy"). Verify count is exactly 54 (the test in acceptance will catch off-by-one errors).
    - `ISO3_SET: frozenset[str] = frozenset(iso3 for iso3, _ in COUNTRIES)` — used by the click-handler in 02-04 to validate clicked ISO3 BEFORE writing to session_state (prevents the ESH/-99 leak — RESEARCH §"CRITICAL FINDING").
    - `_NAME_BY_ISO3: dict[str, str] = dict(COUNTRIES)` — module-private O(1) lookup backing `name_for`.
    - `def iter_countries() -> list[tuple[str, str]]:` — returns `list(COUNTRIES)` (a fresh list each call so callers can mutate safely). Per D-29.
    - `def name_for(iso3: str) -> str:` — returns `_NAME_BY_ISO3.get(iso3, iso3)`. NEVER raises (RESEARCH §"Concrete file"). The "return iso3 for unknown codes" fallback makes the helper safe as the `format_func` of the selectbox even during transient mismatches.
    - `def warn_if_drifted_from_shapefiles(available_iso3s: Iterable[str]) -> None:` — per D-29. Computes `set(ISO3_SET) - set(available_iso3s)` (in metadata but no shapefile) and `set(available_iso3s) - set(ISO3_SET)` (in shapefiles but not metadata). If either is non-empty, emit ONE `log.warning(...)` line with both sorted lists. Does NOT raise (D-29: tolerate divergence per D-04 spirit). The warning text is the RESEARCH-recommended form: `"Country metadata / shapefile drift detected: in metadata but no shapefile: %s; in shapefiles but not metadata: %s"`.

    Do NOT import from `shapefiles.py` at module load time — `warn_if_drifted_from_shapefiles` takes the iterable as an argument. This is so the wiring decision (where to call it) lives outside `country_metadata.py` and avoids a circular import risk.

    Style: f-strings only in user-facing strings if needed; logging uses `%s` lazy-formatting per RESEARCH and stdlib convention. Match the docstring style of the existing Phase 1 modules (Google-style with Args/Returns/Raises blocks where applicable; see `data/shapefiles.py` for the template).
  </action>
  <verify>
    <automated>uv run python -c "
from mosaic_dashboard.data import country_metadata
import sys
errs = []
xs = country_metadata.iter_countries()
if len(xs) != 54: errs.append(f'expected 54 entries, got {len(xs)}')
if xs[0] != ('AGO', 'Angola'): errs.append(f'first entry should be (AGO, Angola), got {xs[0]}')
if country_metadata.name_for('AGO') != 'Angola': errs.append('name_for AGO failed')
if country_metadata.name_for('ZZZ') != 'ZZZ': errs.append('name_for fallback failed')
if 'AGO' not in country_metadata.ISO3_SET: errs.append('ISO3_SET missing AGO')
if 'ESH' in country_metadata.ISO3_SET: errs.append('ISO3_SET should not contain ESH')
if '-99' in country_metadata.ISO3_SET: errs.append('ISO3_SET should not contain -99')
if 'MUS' not in country_metadata.ISO3_SET: errs.append('ISO3_SET missing MUS')
if 'SYC' not in country_metadata.ISO3_SET: errs.append('ISO3_SET missing SYC')
country_metadata.warn_if_drifted_from_shapefiles([])  # must not raise
if errs:
    print('FAIL'); [print('-', e) for e in errs]; sys.exit(1)
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `src/mosaic_dashboard/data/country_metadata.py` exists.
    - `from mosaic_dashboard.data import country_metadata` succeeds under `uv run`.
    - `len(country_metadata.iter_countries()) == 54`.
    - `country_metadata.iter_countries()[0] == ("AGO", "Angola")` (first entry IS the default per D-26, D-27).
    - `country_metadata.name_for("AGO") == "Angola"` and `country_metadata.name_for("ZZZ") == "ZZZ"` (unknown returns input — never raises).
    - `"AGO" in country_metadata.ISO3_SET` and `"MUS" in country_metadata.ISO3_SET` and `"SYC" in country_metadata.ISO3_SET` and `"ESH" not in country_metadata.ISO3_SET` and `"-99" not in country_metadata.ISO3_SET`.
    - `country_metadata.warn_if_drifted_from_shapefiles([])` returns `None` without raising.
    - `grep -c "^\\s*(\"" src/mosaic_dashboard/data/country_metadata.py` counts at least 54 tuple-entry lines (sanity check on the table; comments excluded).
  </acceptance_criteria>
  <done>
    `country_metadata.iter_countries()` returns 54 tuples in alphabetical-by-name order with Angola first; `name_for`, `ISO3_SET`, and `warn_if_drifted_from_shapefiles` are exported and behave per D-29.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> Python | Reads `pyproject.toml`; no untrusted input. Country names are hard-coded constants in source code. |
| PyPI -> uv.lock | `uv add` fetches three packages + transitive deps over HTTPS. PyPI is the standard trust root for the Python ecosystem; `uv.lock` pins hashes which protects subsequent `uv sync` calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | uv.lock (supply chain) | mitigate | `uv add` writes content-addressed hashes to `uv.lock`; subsequent `uv sync` validates them. No action beyond committing the lock file. |
| T-02-02 | Tampering | country_metadata.py | accept | File is hard-coded source under version control; tampering = a malicious commit, which is caught by code review, not runtime defenses. Single-user local dashboard with no remote write surface. |
| T-02-03 | Information Disclosure | (none) | accept | No PII, no secrets, no env vars introduced by this plan. |
</threat_model>

<verification>
- `uv run python -c "from mosaic_dashboard.config import COUNTRY_SESSION_KEY; from mosaic_dashboard.data import country_metadata; assert len(country_metadata.iter_countries()) == 54"` exits 0.
- `git diff --stat pyproject.toml uv.lock src/mosaic_dashboard/config.py src/mosaic_dashboard/data/country_metadata.py` shows exactly those four paths modified/created and no others.
- DATA-03 still holds: `grep -rE '^(from |import )(requests|httpx|urllib\.request|aiohttp)\b' src/mosaic_dashboard/` returns no matches (Phase 2 doesn't break Phase 1's offline guarantee at the Python layer).
</verification>

<success_criteria>
- B1 (RESEARCH B1-B10): `uv add` updated pyproject.toml + uv.lock; `rm -rf .venv && uv sync` exits 0 (verified post-plan).
- B2 (partial): `country_metadata.iter_countries()` returns 54 entries. The cross-check with `shapefiles.available_countries()` is wired in 02-03 (where it's actually called).
- D-21..D-29 and D-32 (the constant) are present in code.
</success_criteria>

<output>
After completion, create `.planning/phases/02-country-navigation-ssa-map/02-01-SUMMARY.md` capturing:
- The three deps and the resolved versions from `uv.lock` (one-line each).
- The 54-entry table reference (just say "matches RESEARCH §country_metadata.py").
- That `COUNTRY_SESSION_KEY` is exported from `mosaic_dashboard.config` with value `"selected_country_iso3"`.
- That `warn_if_drifted_from_shapefiles` is defined but NOT yet called — wiring lives in 02-03.
- Decisions implemented: D-21, D-22, D-24, D-25, D-26, D-27, D-28, D-29 (helpers exist), D-32 (constant defined).
</output>
