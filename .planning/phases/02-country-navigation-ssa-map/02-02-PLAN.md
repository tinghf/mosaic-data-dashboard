---
phase: 02-country-navigation-ssa-map
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/mosaic_dashboard/data/shapefiles.py
autonomous: true
requirements:
  - MAP-01
  - MAP-02
must_haves:
  truths:
    - "load_africa_geometry() returns a geopandas.GeoDataFrame with 54 rows when AFRICA_ADM0.shp is present, with the columns iso_a3, name, and geometry (D-38)"
    - "load_africa_geometry() returns an EMPTY GeoDataFrame (not raise) when AFRICA_ADM0.shp is missing and logs a warning (D-39, mirrors D-10)"
    - "load_country_geometry('AGO') returns a GeoDataFrame with one row containing columns ADM0 and geometry (D-38)"
    - "Both geometry loaders are cached via @st.cache_data keyed on (path_str, mtime) — same pattern as Phase 1 D-18"
    - "Existing Phase-1 functions available_countries(), load_africa(), load_country(country) are byte-for-byte unchanged in their public behavior (D-20, D-38, D-40)"
    - "When AFRICA_ADM0 shapefile lacks the iso_a3 attribute, the loader raises SchemaMismatchError(dataset='shapefiles/AFRICA_ADM0', ...) — strict per D-39, mirroring D-12"
  artifacts:
    - path: "src/mosaic_dashboard/data/shapefiles.py"
      provides: "Geometry loaders (additive) on top of Phase 1's Path-only metadata loaders"
      exports: ["load_africa_geometry", "load_country_geometry", "AFRICA_REQUIRED_COLUMNS", "COUNTRY_REQUIRED_COLUMNS"]
  key_links:
    - from: "src/mosaic_dashboard/data/shapefiles.py:load_africa_geometry"
      to: "geopandas.read_file"
      via: "private _read_africa_geometry_cached(path, mtime) helper"
      pattern: "@st.cache_data"
    - from: "src/mosaic_dashboard/data/shapefiles.py:load_country_geometry"
      to: "geopandas.read_file"
      via: "private _read_country_geometry_cached(path, mtime) helper"
      pattern: "@st.cache_data"
    - from: "src/mosaic_dashboard/data/shapefiles.py"
      to: "mosaic_dashboard.data._schema.require_columns"
      via: "schema enforcement on geometry reads (D-39 mirrors D-12)"
      pattern: "require_columns\\(gdf, AFRICA_REQUIRED_COLUMNS"
---

<objective>
Extend `src/mosaic_dashboard/data/shapefiles.py` with two NEW geometry-reading functions — `load_africa_geometry()` and `load_country_geometry(country)` — that wrap `geopandas.read_file(...)`, cache via `@st.cache_data` keyed on `(path, mtime)`, and honor the same empty-state + strict-schema contract Phase 1 established. **Additive only** — existing functions (`available_countries`, `load_africa`, `load_country`, `_load_shape_metadata`) keep their Path-only semantics byte-for-byte unchanged.

Purpose: 02-04's `pages/01_SSA_Map.py` calls `load_africa_geometry()` to render the SSA map. Phase 5 (LAYER-06) will call `load_country_geometry(iso3)` for the per-country shape view. Implements D-38, D-39, D-40.

Output:
- `src/mosaic_dashboard/data/shapefiles.py` with the new geometry loaders + their private cached helpers + two module-level required-column frozensets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-country-navigation-ssa-map/02-CONTEXT.md
@.planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md
@.planning/phases/02-country-navigation-ssa-map/02-01-PLAN.md
@CLAUDE.md
@src/mosaic_dashboard/data/shapefiles.py
@src/mosaic_dashboard/data/_schema.py
@src/mosaic_dashboard/data/errors.py
@src/mosaic_dashboard/config.py

<interfaces>
<!-- Pre-existing surface this plan extends (must remain unchanged per D-20/D-38/D-40). -->

From src/mosaic_dashboard/data/shapefiles.py (Phase 1):
```python
SHAPEFILES_SUBDIR: str = "shapefiles"
SHAPEFILE_EXTENSIONS: tuple[str, ...] = ("shp", "shx", "dbf", "prj")

def available_countries() -> list[str]: ...   # UNCHANGED
def load_africa() -> pd.DataFrame: ...          # UNCHANGED (Path-only metadata)
def load_country(country: str) -> pd.DataFrame: ...  # UNCHANGED (Path-only metadata)
def _load_shape_metadata(stem: str) -> pd.DataFrame: ...   # UNCHANGED (private)
```

From src/mosaic_dashboard/data/_schema.py:
```python
def require_columns(df: pd.DataFrame, required: Iterable[str], dataset: str) -> None: ...
# Note: signature types df as pd.DataFrame, but gpd.GeoDataFrame is a subclass
# of pd.DataFrame at runtime — require_columns(geo_df, ...) works without
# changes (geopandas.GeoDataFrame inherits .columns from pandas).
```

From src/mosaic_dashboard/data/errors.py:
```python
class SchemaMismatchError(DataLayerError): ...   # raised when required cols absent
```

From src/mosaic_dashboard/config.py:
```python
def resolve_data_root() -> Path: ...   # path resolution per D-04
```

<!-- Interfaces this plan CREATES, consumed by 02-04 and Phase 5. -->

In src/mosaic_dashboard/data/shapefiles.py (NEW):
```python
AFRICA_REQUIRED_COLUMNS: frozenset[str]   # {"iso_a3", "name", "geometry"}
COUNTRY_REQUIRED_COLUMNS: frozenset[str]  # {"ADM0", "geometry"}

def load_africa_geometry() -> gpd.GeoDataFrame: ...
def load_country_geometry(country: str) -> gpd.GeoDataFrame: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add load_africa_geometry() to shapefiles.py with @st.cache_data + empty-state + schema check</name>
  <files>src/mosaic_dashboard/data/shapefiles.py</files>
  <read_first>
    - .planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md §"shapefiles.py Geometry Extension (copy-pasteable skeleton)" — has the entire function body verbatim
    - .planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md §"GeoDataFrame Inspection (real DBF schema)" — confirms `iso_a3` is the lowercase-underscore column name (NOT `ISO3` or `ADM0_A3`); also explains the EPSG:4326 CRS and the 54-row count
    - .planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md §"Caching Strategy for GeoDataFrames" — confirms @st.cache_data (not @st.cache_resource), no hash_funcs needed
    - .planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md §"Pitfalls" P11 (pyogrio strictness if .shx missing)
    - .planning/phases/02-country-navigation-ssa-map/02-CONTEXT.md D-18, D-38, D-39, D-40
    - src/mosaic_dashboard/data/shapefiles.py (existing module — to identify the INSERTION point for new code; existing functions MUST remain byte-for-byte unchanged per D-38)
    - src/mosaic_dashboard/data/_schema.py (require_columns signature)
    - src/mosaic_dashboard/data/errors.py (SchemaMismatchError signature)
  </read_first>
  <action>
    Edit `src/mosaic_dashboard/data/shapefiles.py` ADDITIVELY (D-38: existing functions unchanged):

    (a) Update the module docstring's "Public surface" block to add two new bullets for `load_africa_geometry` and `load_country_geometry`. Keep the existing docstring lines intact — just extend the surface enumeration. Add a one-sentence note that the geometry loaders honor the same `@st.cache_data(path, mtime)` pattern as the rest of the data layer (D-18) and the same empty-state contract (D-39).

    (b) Add these imports immediately AFTER the existing imports (do NOT touch existing imports):
    ```
    import geopandas as gpd
    import streamlit as st
    from mosaic_dashboard.data._schema import require_columns
    from mosaic_dashboard.data.errors import SchemaMismatchError
    ```
    Note: `pd` is already imported; do not re-import. `Path` is already imported; do not re-import.

    (c) Add a module-level constant after the existing `_METADATA_COLUMNS`:
    ```
    AFRICA_REQUIRED_COLUMNS: frozenset[str] = frozenset({"iso_a3", "name", "geometry"})
    ```
    With a `#:` docstring comment explaining: the AFRICA_ADM0.dbf is the Natural Earth schema (RESEARCH §3), the ISO3 column name is the lowercase-underscore `iso_a3` (NOT `ISO3` or `ADM0_A3` — RESEARCH P7), `name` drives map hover tooltips per UI-SPEC, `geometry` is the polygon column added by geopandas.

    (d) Add `load_africa_geometry()` and its private cached helper at the END of the module (after `_load_shape_metadata`). Copy the function bodies from RESEARCH §"`load_africa_geometry()` — full SSA GeoDataFrame" with these exact behaviors:

    - `load_africa_geometry() -> gpd.GeoDataFrame`:
      - `root = resolve_data_root()`
      - `shp_path = root / SHAPEFILES_SUBDIR / "AFRICA_ADM0.shp"`
      - **Defensive existence check (RESEARCH P11):** check that all three of `.shp`, `.shx`, `.dbf` exist (not just `.shp`). If any are missing, `log.warning(...)` with the missing path and return the empty GeoDataFrame from `_empty_africa_geometry()`. Pyogrio raises hard on missing `.shx` — this guard avoids the raise and surfaces the same empty-state contract D-39 specifies.
      - `mtime = shp_path.stat().st_mtime` (only after the existence check)
      - `return _read_africa_geometry_cached(str(shp_path), mtime)` — the `str(...)` cast is per RESEARCH §"Caching Strategy" (Phase 1's convention; avoids `hash_funcs` for Path).
      - Module docstring explains the empty-state contract, the schema-strict contract, and the cache key. Use the docstring from RESEARCH verbatim (8-12 lines, mirrors Phase 1 docstring style).

    - `_read_africa_geometry_cached(path: str, mtime: float) -> gpd.GeoDataFrame`:
      - Decorated `@st.cache_data(show_spinner=False)` (RESEARCH §"Caching Strategy").
      - Body: `gdf = gpd.read_file(path)`; `require_columns(gdf, AFRICA_REQUIRED_COLUMNS, dataset="shapefiles/AFRICA_ADM0")`; `return gdf`.
      - `mtime` is in the signature SOLELY to participate in the cache key (the docstring must state this explicitly, mirroring Phase 1's `_who_annual_cached(path, mtime)` style; future readers must understand why `mtime` is passed but not used in the body).

    - `_empty_africa_geometry() -> gpd.GeoDataFrame` (private helper, not exported):
      - Returns `gpd.GeoDataFrame({"iso_a3": pd.Series(dtype="object"), "name": pd.Series(dtype="object")}, geometry=gpd.GeoSeries(dtype="geometry"), crs="EPSG:4326")`.
      - Natural Earth ships in EPSG:4326 (RESEARCH §3); the empty frame matches that CRS so callers don't need to special-case it.

    Do NOT change `_load_shape_metadata`, `load_africa`, `load_country`, or `available_countries`. Do NOT change `SHAPEFILES_SUBDIR`, `SHAPEFILE_EXTENSIONS`, `_ISO3_PREFIX_RE`, `_AFRICA_STEM`, or `_METADATA_COLUMNS`. Their contracts are locked per D-20/D-40.
  </action>
  <verify>
    <automated>uv run python -c "
from mosaic_dashboard.data import shapefiles
import sys
errs = []
# Existing surface still works (D-20, D-38, D-40)
cs = shapefiles.available_countries()
if not (isinstance(cs, list) and len(cs) == 54):
    errs.append(f'available_countries() expected 54-element list, got {type(cs).__name__} len={len(cs) if hasattr(cs,\"__len__\") else \"?\"}')
m = shapefiles.load_africa()
if 'extension' not in m.columns:
    errs.append('load_africa() metadata DataFrame lost its columns')
# New surface
gdf = shapefiles.load_africa_geometry()
if gdf is None:
    errs.append('load_africa_geometry returned None')
elif len(gdf) != 54:
    errs.append(f'load_africa_geometry expected 54 rows, got {len(gdf)}')
elif not {'iso_a3', 'name', 'geometry'}.issubset(set(gdf.columns)):
    errs.append(f'load_africa_geometry missing required columns. got: {list(gdf.columns)[:10]}')
elif str(gdf.crs) not in ('EPSG:4326', 'epsg:4326'):
    errs.append(f'expected EPSG:4326 CRS, got {gdf.crs}')
# constants exported
if not hasattr(shapefiles, 'AFRICA_REQUIRED_COLUMNS'):
    errs.append('AFRICA_REQUIRED_COLUMNS not exported')
if errs:
    print('FAIL'); [print('-', e) for e in errs]; sys.exit(1)
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '^def load_africa_geometry' src/mosaic_dashboard/data/shapefiles.py` returns one match.
    - `grep -E '^def _read_africa_geometry_cached' src/mosaic_dashboard/data/shapefiles.py` returns one match.
    - `grep -E 'AFRICA_REQUIRED_COLUMNS' src/mosaic_dashboard/data/shapefiles.py` returns at least two matches (definition + use).
    - `grep -E '@st\.cache_data' src/mosaic_dashboard/data/shapefiles.py` returns at least one match (the new private cached helper).
    - `grep -E '"iso_a3"' src/mosaic_dashboard/data/shapefiles.py` returns at least one match (the column name is the lowercase-underscore form — RESEARCH P7).
    - `uv run python -c "from mosaic_dashboard.data.shapefiles import load_africa_geometry; gdf = load_africa_geometry(); assert len(gdf) == 54 and {'iso_a3','name','geometry'}.issubset(set(gdf.columns))"` exits 0.
    - Existing Phase-1 contract still works: `uv run python -c "from mosaic_dashboard.data.shapefiles import available_countries, load_africa, load_country; assert len(available_countries()) == 54; assert 'extension' in load_africa().columns; assert 'extension' in load_country('AGO').columns"` exits 0.
  </acceptance_criteria>
  <done>
    `load_africa_geometry()` returns a 54-row GeoDataFrame with columns `iso_a3`, `name`, `geometry` in EPSG:4326; missing-shapefile path returns an empty GeoDataFrame and logs a warning; existing Phase-1 functions remain unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add load_country_geometry(country) to shapefiles.py with the same pattern</name>
  <files>src/mosaic_dashboard/data/shapefiles.py</files>
  <read_first>
    - .planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md §"`load_country_geometry(iso3)` — single-country GeoDataFrame" (verbatim skeleton)
    - .planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md §"Per-country `XXX_ADM0.dbf` schema" (single field `ADM0` + geometry; no ISO3 IN the DBF — ISO3 comes from the filename prefix)
    - .planning/phases/02-country-navigation-ssa-map/02-CONTEXT.md D-38, D-39
    - src/mosaic_dashboard/data/shapefiles.py (after Task 1 lands — Task 2 appends to the same module; you must not duplicate Task 1's imports or `_empty_africa_geometry`)
  </read_first>
  <action>
    Append to `src/mosaic_dashboard/data/shapefiles.py`:

    (a) Add the second module-level constant immediately after `AFRICA_REQUIRED_COLUMNS`:
    ```
    COUNTRY_REQUIRED_COLUMNS: frozenset[str] = frozenset({"ADM0", "geometry"})
    ```
    Comment: per-country DBFs ship with one column `ADM0` (the country name); geometry is added by geopandas. The ISO3 itself is NOT in the DBF — it lives in the filename prefix (verified RESEARCH §3 "Per-country XXX_ADM0.dbf schema"). Phase 5 LAYER-06 is the consumer.

    (b) Add `load_country_geometry(country: str) -> gpd.GeoDataFrame`, `_read_country_geometry_cached(path: str, mtime: float) -> gpd.GeoDataFrame`, and `_empty_country_geometry() -> gpd.GeoDataFrame` at the END of the module, mirroring the Task-1 trio for AFRICA:

    - `load_country_geometry(country)`:
      - `stem = country.upper()` — defensive normalization (matches the existing `load_country(country)` pattern in this same file).
      - `root = resolve_data_root()`
      - `shp_path = root / SHAPEFILES_SUBDIR / f"{stem}_ADM0.shp"`
      - Same defensive existence check as Task 1: if `.shp` / `.shx` / `.dbf` are not all present, log a warning naming the missing files and return `_empty_country_geometry()`.
      - Otherwise: `mtime = shp_path.stat().st_mtime`; `return _read_country_geometry_cached(str(shp_path), mtime)`.
      - Docstring explains: caller passes ISO3 (e.g., "AGO"); returned DataFrame has columns `ADM0` (name only — per-country DBFs lack ISO3) and `geometry`. Missing file → empty GeoDataFrame + warning. Phase 5 LAYER-06 is the primary consumer (RESEARCH §"Open Questions" Q4 — Phase 2 adds the function but doesn't wire it).

    - `_read_country_geometry_cached(path: str, mtime: float)`:
      - `@st.cache_data(show_spinner=False)`.
      - `gdf = gpd.read_file(path)`; `require_columns(gdf, COUNTRY_REQUIRED_COLUMNS, dataset=f"shapefiles/{Path(path).stem}")`; `return gdf`. Note `Path(path).stem` gives e.g. `"AGO_ADM0"` so the schema error message names the offending shape.

    - `_empty_country_geometry()`:
      - Returns `gpd.GeoDataFrame({"ADM0": pd.Series(dtype="object")}, geometry=gpd.GeoSeries(dtype="geometry"), crs="EPSG:4326")`.

    Note: RESEARCH §"Open Questions" Q4 explicitly says Phase 2 adds the function but does NOT wire it into any view (Phase 5 LAYER-06 will consume it). This task's `<verify>` block uses a smoke test only.
  </action>
  <verify>
    <automated>uv run python -c "
from mosaic_dashboard.data import shapefiles
import sys
errs = []
# Smoke test load_country_geometry for AGO (known to exist on disk per Phase 1 verification)
gdf = shapefiles.load_country_geometry('AGO')
if gdf is None:
    errs.append('load_country_geometry returned None')
elif len(gdf) != 1:
    errs.append(f'load_country_geometry(AGO) expected 1 row, got {len(gdf)}')
elif not {'ADM0', 'geometry'}.issubset(set(gdf.columns)):
    errs.append(f'load_country_geometry(AGO) missing required columns. got: {list(gdf.columns)}')
# ISO3 normalization (lowercase input must work)
gdf_lc = shapefiles.load_country_geometry('ago')
if len(gdf_lc) != 1:
    errs.append('load_country_geometry case-normalization failed')
# Missing country -> empty + warning (no raise)
empty = shapefiles.load_country_geometry('ZZZ')
if empty is None:
    errs.append('load_country_geometry(ZZZ) returned None instead of empty GeoDataFrame')
elif len(empty) != 0:
    errs.append(f'load_country_geometry(ZZZ) expected 0 rows, got {len(empty)}')
elif 'ADM0' not in empty.columns:
    errs.append('empty country GeoDataFrame missing ADM0 column')
# Constants exported
if not hasattr(shapefiles, 'COUNTRY_REQUIRED_COLUMNS'):
    errs.append('COUNTRY_REQUIRED_COLUMNS not exported')
# Phase 1 contracts STILL unchanged
cs = shapefiles.available_countries()
if len(cs) != 54: errs.append(f'available_countries() drift: got {len(cs)}')
m = shapefiles.load_africa()
if 'extension' not in m.columns: errs.append('load_africa() metadata schema drift')
if errs:
    print('FAIL'); [print('-', e) for e in errs]; sys.exit(1)
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '^def load_country_geometry' src/mosaic_dashboard/data/shapefiles.py` returns one match.
    - `grep -E '^def _read_country_geometry_cached' src/mosaic_dashboard/data/shapefiles.py` returns one match.
    - `grep -E 'COUNTRY_REQUIRED_COLUMNS' src/mosaic_dashboard/data/shapefiles.py` returns at least two matches (definition + use).
    - `uv run python -c "from mosaic_dashboard.data.shapefiles import load_country_geometry; gdf = load_country_geometry('AGO'); assert len(gdf) == 1 and 'ADM0' in gdf.columns and 'geometry' in gdf.columns"` exits 0.
    - `uv run python -c "from mosaic_dashboard.data.shapefiles import load_country_geometry; gdf = load_country_geometry('ZZZ'); assert gdf.empty"` exits 0 (missing country → empty, no raise).
    - Phase 1 functions still untouched: `git diff src/mosaic_dashboard/data/shapefiles.py` shows ONLY additions (lines starting with `+`) for the geometry functions, NO modifications to lines inside `available_countries`, `load_africa`, `load_country`, or `_load_shape_metadata` (the only acceptable in-place change to Phase 1 territory is the module docstring's "Public surface" block extension and the import block).
  </acceptance_criteria>
  <done>
    `load_country_geometry("AGO")` returns a 1-row GeoDataFrame with `ADM0` and `geometry`; missing country returns an empty GeoDataFrame; `_read_country_geometry_cached` is `@st.cache_data`-decorated and matches the AFRICA pattern.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> Python | `geopandas.read_file()` reads from `~/MOSAIC/MOSAIC-data/processed/shapefiles/*.shp` — read-only path under user's HOME. Per CLAUDE.md §2 the data root is READ-ONLY; no write surface exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | shapefiles loaded from disk | accept | Single-user local; shapefiles are read-only via geopandas/pyogrio. Path traversal not exposed — `load_country_geometry` accepts an ISO3, normalizes to upper case, and constructs the path via `root / SHAPEFILES_SUBDIR / f"{stem}_ADM0.shp"`. The ISO3 is provided by Python source (country_metadata) or by the click handler (validated against ISO3_SET) — not by HTTP/CLI input. |
| T-02-05 | Denial of Service | massive shapefile load | mitigate | `@st.cache_data` caches the parsed GeoDataFrame; first read ≈ 50–200 ms (RESEARCH §"Cache size / eviction"), subsequent reads are O(1). DoS surface is negligible for a single-user local dashboard. |
| T-02-06 | Information Disclosure | (none) | accept | No PII, no secrets. Shapefiles contain country names + polygons (public Natural Earth data). |
</threat_model>

<verification>
- `uv run python -c "from mosaic_dashboard.data.shapefiles import load_africa_geometry, load_country_geometry; gdf=load_africa_geometry(); assert len(gdf)==54; gdf2=load_country_geometry('BDI'); assert len(gdf2)==1"` exits 0.
- D-20 / D-38 / D-40 hold: existing public functions in `shapefiles.py` unchanged. Confirm with `git diff src/mosaic_dashboard/data/shapefiles.py` — only additions in the "geometry" section and at the top (imports + docstring + constants).
- DATA-03 still holds: `grep -rE '^(from |import )(requests|httpx|urllib\.request|aiohttp)\b' src/mosaic_dashboard/` returns no matches. Geopandas reads from local files only.
</verification>

<success_criteria>
- B2 (RESEARCH §"Acceptance"): `load_africa_geometry()` returns 54 rows; `load_country_geometry("AGO")` returns 1 row.
- D-38: function additions are surgical; existing functions unchanged.
- D-39: missing-file path returns empty GeoDataFrame + warning; schema mismatch raises `SchemaMismatchError`.
- D-40: D-20 honored — Phase-1 callers see no API change.
</success_criteria>

<output>
After completion, create `.planning/phases/02-country-navigation-ssa-map/02-02-SUMMARY.md` capturing:
- The two new functions and their constants (one-line each).
- The pyogrio-strict mitigation (defensive `.shp` + `.shx` + `.dbf` existence check before `gpd.read_file`).
- Confirmation Phase 1's `available_countries()`, `load_africa()`, `load_country()` are unchanged.
- Decisions implemented: D-38, D-39, D-40 (and inherited D-18 caching pattern).
</output>
