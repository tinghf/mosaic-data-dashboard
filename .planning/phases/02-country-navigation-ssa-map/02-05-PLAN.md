---
phase: 02-country-navigation-ssa-map
plan: 05
type: execute
wave: 4
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
  - "02-04"
files_modified:
  - .planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - MAP-01
  - MAP-02
must_haves:
  truths:
    - "All B1-B10 acceptance items from RESEARCH §Validation Architecture are walked through with a recorded PASS/FAIL outcome"
    - "Browser-only acceptance items (B3, B4, B5, B6, B7, B8) are exercised by the user on a live Streamlit run (uv sync && uv run streamlit run src/mosaic_dashboard/app.py)"
    - "Headless acceptance items (B1, B2, B9, B10) are exercised by Claude using the manual probe scripts from RESEARCH §Manual probe scripts"
    - "Phase 2 status is set to READY TO TRANSITION only if all B1-B10 PASS or are explicitly waived by the user"
  artifacts:
    - path: ".planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md"
      provides: "Recorded B1-B10 outcomes with timestamps + user signoff"
      min_lines: 40
  key_links:
    - from: ".planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md"
      to: ".planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md §Validation Architecture"
      via: "B1-B10 numbering and check definitions"
      pattern: "B[0-9]+"
---

<objective>
Walk through the B1-B10 acceptance checks defined in RESEARCH.md §"Validation Architecture (Phase 2 acceptance checks)" to verify the Phase 2 outcome end-to-end. The check is split into a Claude-automated half (B1, B2, B9, B10 — headless probes) and a user-interactive half (B3, B4, B5, B6, B7, B8 — browser walkthrough).

This plan is `autonomous: false` — it contains one `checkpoint:human-verify` task that pauses for the user to drive the live Streamlit app through B3-B8.

Output:
- `.planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md` with per-item PASS/FAIL records and the user's final signoff line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-country-navigation-ssa-map/02-CONTEXT.md
@.planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md
@.planning/phases/02-country-navigation-ssa-map/02-UI-SPEC.md
@.planning/phases/02-country-navigation-ssa-map/02-01-PLAN.md
@.planning/phases/02-country-navigation-ssa-map/02-02-PLAN.md
@.planning/phases/02-country-navigation-ssa-map/02-03-PLAN.md
@.planning/phases/02-country-navigation-ssa-map/02-04-PLAN.md
@.planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Claude-automated probes (B1, B2, B9, B10) and scaffold the ACCEPTANCE.md log</name>
  <files>.planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md</files>
  <read_first>
    - .planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md "Validation Architecture (Phase 2 acceptance checks)" — B1-B10 table with the precise verify procedure for each item
    - .planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md "Manual probe scripts" — copy-pasteable shell snippets for B2
    - .planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md — template for the per-item record format (date, status, evidence)
  </read_first>
  <action>
    Run the headless probes and record the outcomes.

    (a) **B1 — uv add updated pyproject.toml + uv.lock cleanly + cold venv sync.**
        Probe: `git diff pyproject.toml | grep -E '^\\+.*(geopandas|folium|streamlit-folium)'` returns three lines.
        Probe: `rm -rf .venv && uv sync` exits 0 (re-sync from cold) — only run this if `.venv/` is safe to delete; if the user has uncommitted local-only deps the orchestrator should ask first. SAFE in this project (greenfield, no local-only deps).
        Record: PASS if both probes succeed; FAIL with the failing command output otherwise.

    (b) **B2 — country_metadata vs available_countries cross-check.**
        Probe (verbatim from RESEARCH §"Manual probe scripts"):
        ```bash
        uv run python -c "
        from mosaic_dashboard.data import country_metadata, shapefiles
        meta_set = set(iso3 for iso3, _ in country_metadata.iter_countries())
        shape_set = set(shapefiles.available_countries())
        print(f'metadata count: {len(meta_set)}')
        print(f'shapefile count: {len(shape_set)}')
        print(f'in meta, no shape: {meta_set - shape_set}')
        print(f'in shape, no meta: {shape_set - meta_set}')
        "
        ```
        Expected output (per RESEARCH §"CRITICAL FINDING"):
        - metadata count: 54
        - shapefile count: 54
        - in meta, no shape: {'MUS', 'SYC'}
        - in shape, no meta: (depends on Phase 1 — likely empty since available_countries() filters AFRICA_ADM0 out and only walks per-country files)
        Note: the divergence at the polygon level (ESH/-99 in AFRICA_ADM0 DBF but not in per-country files) is surfaced by B10 below, not B2.
        Record: PASS if metadata count is 54 and the divergence matches RESEARCH's prediction; FAIL otherwise (paste the actual output).

    (c) **B9 — Missing AFRICA_ADM0.shp empty-state.**
        Probe: temporarily move the file to its `.bak` form, exercise `load_africa_geometry()` once, then restore:
        ```bash
        mv ~/MOSAIC/MOSAIC-data/processed/shapefiles/AFRICA_ADM0.shp ~/MOSAIC/MOSAIC-data/processed/shapefiles/AFRICA_ADM0.shp.bak
        uv run python -c "from mosaic_dashboard.data import shapefiles; gdf = shapefiles.load_africa_geometry(); print('empty:', gdf.empty, 'rows:', len(gdf))"
        mv ~/MOSAIC/MOSAIC-data/processed/shapefiles/AFRICA_ADM0.shp.bak ~/MOSAIC/MOSAIC-data/processed/shapefiles/AFRICA_ADM0.shp
        ```
        Expected: prints `empty: True rows: 0` and a WARNING log line; restores the file. CRITICAL: the restore step MUST always run, even if the probe fails — wrap in a shell `trap` or two-step the commands so a failure does not leave the user's data root with a renamed file. Per CLAUDE.md §2 the data root is READ-ONLY in the steady state — a single deliberate move-and-restore is the only acceptable exception, and it MUST end with the original layout intact.
        Browser part of B9 (loading the SSA Map page with the file moved and confirming the in-page st.warning banner) is deferred to the human-verify task — the headless probe only confirms the Python-side empty-state contract.
        Record: PASS if the probe exits clean and the file is restored; FAIL with the actual output otherwise.

    (d) **B10 — Drift-warning emission.**
        Probe:
        ```bash
        uv run python -c "
        import logging, sys
        logging.basicConfig(level=logging.WARNING, format='%(levelname)s %(name)s %(message)s', stream=sys.stderr)
        from mosaic_dashboard.data import country_metadata, shapefiles
        country_metadata.warn_if_drifted_from_shapefiles(shapefiles.available_countries())
        print('drift check complete')
        " 2>&1
        ```
        Expected: ONE log line at WARNING level matching `Country metadata / shapefile drift detected:` with `MUS` and `SYC` listed under "in metadata but no shapefile" (since `available_countries()` walks per-country files, both MUS and SYC ARE present — actually the drift the warning surfaces is between the metadata set and the AFRICA_ADM0 DBF, not the per-country files; check RESEARCH B10 expected text and reconcile against the actual output). The recorded outcome should note whether the actual divergence matches RESEARCH's prediction or whether the prediction needs correction.
        Record: PASS if the warning emits and lists the expected divergence sets; FAIL otherwise.

    (e) **Scaffold the ACCEPTANCE log.** Create `.planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md` with:
    - Front-matter: phase id, plan id, date.
    - One section per item B1..B10. For B1, B2, B9, B10 record the actual outcome with command + stdout snippet.
    - For B3, B4, B5, B6, B7, B8 stub a "Pending user walkthrough" line; the human-verify task will fill them in.
    - A footer signoff block: `## Final signoff` with a blank `[ ] approved` checkbox and a date placeholder.

    Output format mirrors `.planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md` (read it as the template).
  </action>
  <verify>
    <automated>test -f .planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md &amp;&amp; grep -cE '^## B[0-9]+' .planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md | grep -E '^([1-9]|10)$' &amp;&amp; ls -la ~/MOSAIC/MOSAIC-data/processed/shapefiles/AFRICA_ADM0.shp &amp;&amp; echo "AFRICA_ADM0.shp restored"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md` exists.
    - The file has exactly 10 `## B*` section headers (B1 through B10) — verify with `grep -cE '^## B[0-9]+' .planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md` returns 10.
    - B1, B2, B9, B10 sections each have a recorded outcome (PASS / FAIL) with command + stdout evidence.
    - B3-B8 sections are stubbed with `Status: Pending user walkthrough`.
    - `~/MOSAIC/MOSAIC-data/processed/shapefiles/AFRICA_ADM0.shp` exists (the B9 move-and-restore left the original layout intact).
    - File ends with a `## Final signoff` section containing an unchecked `[ ] approved` line.
  </acceptance_criteria>
  <done>
    Headless probes (B1, B2, B9, B10) recorded; ACCEPTANCE.md scaffold in place for the user to fill in B3-B8 during the human-verify checkpoint; the data root is untouched at rest.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User walks B3-B8 in a live Streamlit session and signs off Phase 2</name>
  <files>.planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md</files>
  <read_first>
    - .planning/phases/02-country-navigation-ssa-map/02-RESEARCH.md "Validation Architecture" — B3-B8 verify steps verbatim
    - .planning/phases/02-country-navigation-ssa-map/02-UI-SPEC.md "Map Styling Contract" — the four hex colors B7 verifies
    - .planning/phases/01-foundation-data-layer/01-05-ACCEPTANCE.md — format template for recording per-item PASS/FAIL outcomes
  </read_first>
  <action>
    This is a human-verify checkpoint. Pause for the user to drive the live Streamlit app through B3-B8 (the browser-only half of the B1-B10 acceptance). Use the &lt;how-to-verify&gt; block below as the script. As the user reports outcomes line-by-line, append per-item PASS/FAIL records to `.planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md` (Task 1 already scaffolded the file with B3-B8 stubbed as "Pending user walkthrough"). Do NOT make autonomous code changes here; the executor waits for the user to type the resume signal.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md &amp;&amp; awk -v c=0 "/^## B/{c++} END{ if (c &gt;= 10) print "ACCEPTANCE_LOG_COMPLETE"; else print "ACCEPTANCE_LOG_INCOMPLETE c="c }" .planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md &amp;&amp; grep -E "^\[x\] approved|^## Phase 2 status: (READY TO TRANSITION|GAPS PRESENT)" .planning/phases/02-country-navigation-ssa-map/02-05-ACCEPTANCE.md</automated>
  </verify>
  <done>
    All ten B-sections in `02-05-ACCEPTANCE.md` have PASS/FAIL records; the Final signoff section shows `[x] approved` with a timestamp (or, if any FAIL, the file states `## Phase 2 status: GAPS PRESENT` with the failed checks listed). The user has typed `approved` (or described gaps) in chat.
  </done>
  <what-built>
    Phase 2 ships:
    - Three new deps: geopandas 1.1.x, folium 0.20.x, streamlit-folium 0.27.x (per pyproject.toml).
    - A new `src/mosaic_dashboard/data/country_metadata.py` static 54-entry ISO3 -> name table with `iter_countries`, `name_for`, `ISO3_SET`, `warn_if_drifted_from_shapefiles`.
    - New `COUNTRY_SESSION_KEY` constant in `config.py`.
    - Additive geometry loaders in `data/shapefiles.py`: `load_africa_geometry()`, `load_country_geometry(iso3)` — existing Phase 1 functions untouched.
    - Extended `ui/sidebar.py::render()`: second selectbox below the data-root override, bound to `COUNTRY_SESSION_KEY`, format_func renders the name, options are the 54 ISO3s.
    - Updated `app.py` welcome copy + startup drift-warning call.
    - New page `src/mosaic_dashboard/pages/01_SSA_Map.py` rendering the clickable folium SSA choropleth with bidirectional sync to the sidebar.

    Plan 02-05 Task 1 already ran the headless probes (B1, B2, B9, B10).
    This checkpoint walks the browser-only acceptance items (B3, B4, B5, B6, B7, B8).
  </what-built>
  <how-to-verify>
    From a terminal in the repo root:

    ```
    uv sync && uv run streamlit run src/mosaic_dashboard/app.py
    ```

    Open the URL Streamlit prints (typically http://localhost:8501) in a desktop browser at >=1280px viewport width.

    Walk through these checks in order. After each, type one line into the chat: `B3 PASS`, `B4 PASS`, etc., or paste the failure mode you observed. The orchestrator will update `02-05-ACCEPTANCE.md` with your outcomes.

    **B3 — SSA Map page reachable from sidebar.**
    1. Open the app. Sidebar should list (top-down): the "Data root override" text input, a divider, the "Country" selectbox, then a Streamlit-auto-discovered page list with "Data Status" and "SSA Map" entries.
    2. Click "SSA Map".
    3. The page should render with the title "SSA Map" and the subtitle "Click a country to select it, or use the Country picker in the sidebar."
    Expected: page loads, title + subtitle visible, no traceback.

    **B4 — Selectbox writes ISO3 to session_state.**
    1. On any page (entry, Data Status, or SSA Map), open the sidebar "Country" selectbox.
    2. Pick "Burundi" from the dropdown.
    3. Navigate to the SSA Map page (or stay on it if already there).
    4. The "Selected: Burundi (BDI)" caption appears under the subtitle.
    5. The Burundi polygon is highlighted with the red fill + thicker stroke; other countries remain neutral gray.
    Expected: caption text matches `Selected: Burundi (BDI)`; Burundi polygon visibly highlighted.

    **B5 — Click on the map writes ISO3 to session_state.**
    1. With Burundi selected from B4, on the SSA Map page click on Angola (large country on the southwest coast).
    2. After Streamlit's rerun: the sidebar selectbox now shows "Angola"; the page caption now reads "Selected: Angola (AGO)"; the Angola polygon is now highlighted (and Burundi reverts to gray).
    Expected: all three feedbacks (sidebar, caption, highlight) update in the same rerun cycle.

    **B6 — Selectbox visually updates after a map click (subsumed by B5).**
    Confirmation step: after the click in B5, open the sidebar selectbox — its current value should be "Angola" (not "Burundi"). This is the bidirectional half of MAP-02.
    Expected: selectbox current value matches the just-clicked country.

    **B7 — Map highlight uses the UI-SPEC colors.**
    Change the picker (or click) to five different countries (e.g., AGO, BDI, EGY, ZAF, NGA). For each, verify the highlighted country has a red-ish fill (`#FF4B4B`) with a darker red stroke (`#B71C1C`) at 3px width, while unselected countries have a light gray fill (`#F0F2F6`) with a neutral gray stroke (`#9AA0A6`) at 1px width.
    Expected: red highlight visibly differs from gray default; thicker stroke is visible by eye.

    **B8 — Offline-with-cached-tiles works.**
    1. Note that this check assumes the CartoDB tiles have been fetched at least once in this browser session (so they are in the browser HTTP cache). If you have not interacted with the map yet, pan/zoom once to populate the cache.
    2. Disconnect network: in WSL2, `sudo ip link set <iface> down` is the canonical incantation; on macOS toggle Wi-Fi off; on Windows disable the active adapter.
    3. Browser hard-reload the SSA Map page (Ctrl+Shift+R or Cmd+Shift+R).
    4. Confirm: (a) page still loads; (b) country polygons render (they are SVG generated from local geometry — DATA-03 holds); (c) base map tiles may be cached (rendered) or blank (white background) depending on cache state — EITHER outcome is acceptable per RESEARCH §"Offline behavior" and PROJECT.md spirit.
    5. Reconnect network.
    Expected: page renders polygons regardless of tile cache state; no Python traceback.
    Note: B8 partially also covers the offline path mentioned in DATA-03 — formal coverage of DATA-03 was Phase 1's A4.

    **Reporting:** type your results one line at a time, e.g.:
    ```
    B3 PASS
    B4 PASS
    B5 PASS
    B6 PASS
    B7 PASS
    B8 PASS
    ```
    Or describe what went wrong if any fail.
  </how-to-verify>
  <resume-signal>
    Type `approved` after walking B3-B8 and recording any failures. The orchestrator will:
    - Update `02-05-ACCEPTANCE.md` with the B3-B8 outcomes you reported.
    - If all B1-B10 PASS, set Phase 2 status to READY TO TRANSITION.
    - If any FAIL, the orchestrator will either route to `/gsd-plan-phase --gaps` for closure plans or ask you to triage the failure.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem (data root) -> Python (B9 probe) | B9 temporarily renames `~/MOSAIC/MOSAIC-data/processed/shapefiles/AFRICA_ADM0.shp` to `.bak` and restores it. This is the ONLY write under the data root permitted in the project (per CLAUDE.md §2 "data root is READ-ONLY") — and only for a single probe step. The restore step must always run. |
| browser -> Streamlit local dev server | User exercises B3-B8 against `localhost:8501`. No external attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | data-root state (during B9 move-and-restore) | mitigate | Use a strict two-step probe (move -> probe -> restore). If the probe Python exits non-zero, the shell still executes the restore mv. The orchestrator MUST verify the file exists at the end of Task 1 (acceptance criteria checks `ls AFRICA_ADM0.shp`). |
</threat_model>

<verification>
- ACCEPTANCE.md exists and has all 10 B-sections.
- `~/MOSAIC/MOSAIC-data/processed/shapefiles/AFRICA_ADM0.shp` exists at the end (B9 probe restores).
- User has reported B3-B8 outcomes via the resume signal; orchestrator has logged them.
- All B1-B10 are either PASS or have a recorded failure mode + triage action.
</verification>

<success_criteria>
- All 4 Phase 2 success criteria from ROADMAP.md verified:
  1. Single country picker drives every panel (NAV-01) → B4.
  2. SSA map rendered from AFRICA_ADM0 with clickable countries (MAP-01) → B3, B5.
  3. Map highlights selected country; dropdown and map stay in sync bidirectionally (MAP-02) → B5, B6, B7.
  4. Country persists across view switches (NAV-02) → implicitly via B4 + sidebar pattern; explicit follow-up if any phase 3 view drops the value.
- All B1-B10 from RESEARCH recorded.
- User has signed off in `02-05-ACCEPTANCE.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-country-navigation-ssa-map/02-05-SUMMARY.md` capturing:
- B1-B10 status table (one row per item).
- The user's signoff line + timestamp.
- Any FAIL items routed to gap closure (if any).
- Phase 2 set to READY TO TRANSITION if all PASS.
</output>
